# ISS-005: Reduce remaining byte drift by trace-derived classes

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: area/parity, area/byte-drift, tests, agent
- Assignee: unassigned
- Created: 2026-06-03
- Updated: 2026-06-04
- External ref: WGSL-283

## Description
After recent arena fixes, the external naga-oil compose gate passes with known drift allowed: `cases=150 comparable=149 oracle-blocked=1 writer-exact=149 writer-drift=0 byte-exact=94 byte-drift=55`. These byte drift cases remain known incompatibilities. They should be reduced by solving shared root causes discovered through module, body, and byte-fragment traces.

## Design
Do not select files only because they look small. Group drift cases by trace divergence class, fix one class at a time, and update `external_naga_oil_compose_byte_drift.tsv` and `external_naga_oil_compose_drift_taxonomy.tsv` only from observed gate output.

## Acceptance Criteria
- [ ] Each fixed drift class references the trace issue that identified its root cause.
- [ ] Every manifest update comes from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- [ ] `MOON_WGSL_ALLOW_KNOWN_DRIFT=1 bash tools/check_external_naga_oil_compose_parity.sh` passes after each class fix.
- [ ] `bash tools/check_external_naga_oil_drift_taxonomy.sh` and `moon test` pass after each class fix.

## Relationships
- Depends on: ISS-004
- Parent: ISS-001
- Related: none
- Discovered from: ISS-001

## Notes
- 2026-06-03: Current state is not fully byte-compatible with upstream. `writer-drift=0` should be preserved while reducing `byte-drift=55`.
- 2026-06-03: Started with ISS-004 trace-derived `local-declaration-name-order` class. First representative: `assets/shaders/array_texture.wgsl`, function `point_light...`, upstream first drift `derived_input_2` vs Moon `diffuse`.
- 2026-06-03: Replaced function-wide source-var predeclare with Naga-like lowering: const-var initializer expressions are warmed at function entry, while source-var local handles are predeclared from the first direct var-bearing statement through the current block suffix. Representative writer trace is back to `20/20`, `gradient.wgsl` remains byte-exact, and `array_texture.wgsl` / `pbr.wgsl` first drifts advanced from local declaration order to `_e46` vs `_e47` expression temp numbering. External compose still has `byte-drift=55`, so ISS-005 remains open.
- 2026-06-04: Fixed the next `point_light` trace-derived pointer-call class: user function pointer arguments now lower `&lvalue` to the lvalue handle, skip pointer lvalue access-chain materialization in call emit ranges, preserve pointer formatting in call emitters, and validate addressable value handles against pointer pointee types. The ad hoc `bevy-pbr-point-light` trace no longer drifts on the extra `AddressOf` / `_e46` vs `_e47` class, but now exposes a later expression/statement ordering drift around `enable_diffuse`. Official representative trace remains `20/20`; external compose remains `writer-drift=0 byte-drift=55`, with byte drift hashes refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next additive-product arena ordering class exposed by `point_light`: when an additive parent has multiplicative binary subtrees on both sides and neither side contains calls, the lowerer now preserves source-order subtree allocation instead of pre-lowering the higher-precedence right subtree. This aligns `diffuse * derived_input.NdotL + specular_light * specular_derived_input.NdotL` with upstream. External compose still has `byte-drift=55`; refreshed hashes now show the representative `point_light` first drift moved to later return temporary numbering (`_e84/_e86` vs `_e83/_e85`).
- 2026-06-04: Fixed the next binary-chain root predeclare class: pure left-associative binary chains now predeclare mutable source-local reference roots in source order before lowering loads and binary expressions. This aligns the minimal upstream trace for `lhs * factor * rhs` and removes the expression-order drift in the ad hoc `bevy-pbr-point-light` trace. External compose still has `writer-drift=0 byte-drift=55`; only the existing byte-drift hashes changed and were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next constructor/subtract-chain arena classes exposed by `spot_light`: constructor arguments with literal holes now lower non-literal access arguments before delayed literals while preserving component handles, and `literal - product - product` chains now append the higher-precedence product before the literal and keep the left subtract chain before the later product. The ad hoc `bevy-array-texture-spot-light` trace now has no expression-order drift; only a bindings normalization diff remains in that focused trace. External compose still has `writer-drift=0 byte-drift=55`, with `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `point_light` to `directional_light` generated temp numbering (`_e42` vs `_e41`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next compound-assignment RHS reference-root ordering class exposed by `directional_light`: compound assignments whose RHS mixes member access with source-local identifiers now predeclare RHS source-local roots before target-value loads. This aligns `color *= (*light).color.rgb * texture_sample` without applying the rule to plain arithmetic cases such as `mix_oklch`. External compose still has `writer-drift=0 byte-drift=55`, with `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `directional_light` to `search_for_blockers_in_shadow_map` generated temp numbering (`_e44` vs `_e35`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `search_for_blockers_in_shadow_map` drift class: function lowering now inlines static elements of global const arrays into the function arena, so `D3D_SAMPLE_POINT_POSITIONS[N]` lowers like upstream as fresh literal/compose expressions instead of `Constant + AccessIndex`. Compound assignment RHS predeclaration was also narrowed to root-call argument roots, aligning `sum += search_for_blockers_in_shadow_map_hardware(...)` without regressing `flags |= u32(...) * CONST` cases. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `search_for_blockers_in_shadow_map` to `random_rotation_matrix` generated temp numbering (`_e4` vs `_e5`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.

## Close Notes
