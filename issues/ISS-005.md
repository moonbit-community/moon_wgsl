# ISS-005: Reduce remaining byte drift by trace-derived classes

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: area/parity, area/byte-drift, tests, agent
- Assignee: unassigned
- Created: 2026-06-03
- Updated: 2026-06-05
- External ref: WGSL-283

## Description
After recent arena fixes, the external naga-oil compose gate passes with known drift allowed: `cases=150 comparable=149 oracle-blocked=1 writer-exact=149 writer-drift=0 byte-exact=96 byte-drift=53`. These byte drift cases remain known incompatibilities. They should be reduced by solving shared root causes discovered through module, body, and byte-fragment traces.

## Design
Do not select files only because they look small. Group drift cases by trace divergence class, fix one class at a time, and update `external_naga_oil_compose_byte_drift.tsv` and `external_naga_oil_compose_drift_taxonomy.tsv` only from observed gate output.

## Acceptance Criteria
- [ ] Each fixed drift class references the trace issue that identified its root cause.
- [ ] Every manifest update comes from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- [ ] `MOON_WGSL_ALLOW_KNOWN_DRIFT=1 bash tools/check_external_naga_oil_compose_parity.sh` passes after each class fix.
- [ ] `bash tools/check_external_naga_oil_drift_taxonomy.sh` and `moon test` pass after each class fix.

## Relationships
- Depends on: ISS-004
- Parent: ISS-001
- Related: none
- Discovered from: ISS-001

## Notes
- 2026-06-03: Current state is not fully byte-compatible with upstream. `writer-drift=0` should be preserved while reducing known byte drift.
- 2026-06-05: Legacy tracker migration note: this issue continues the unfinished WGSL-283 byte-drift burn-down work. Structural replacement of rule-patch lowering is split into ISS-007 through ISS-014; final strict byte parity is tracked by ISS-015.
- 2026-06-03: Started with ISS-004 trace-derived `local-declaration-name-order` class. First representative: `assets/shaders/array_texture.wgsl`, function `point_light...`, upstream first drift `derived_input_2` vs Moon `diffuse`.
- 2026-06-03: Replaced function-wide source-var predeclare with Naga-like lowering: const-var initializer expressions are warmed at function entry, while source-var local handles are predeclared from the first direct var-bearing statement through the current block suffix. Representative writer trace is back to `20/20`, `gradient.wgsl` remains byte-exact, and `array_texture.wgsl` / `pbr.wgsl` first drifts advanced from local declaration order to `_e46` vs `_e47` expression temp numbering. External compose still has `byte-drift=55`, so ISS-005 remains open.
- 2026-06-04: Fixed the next `point_light` trace-derived pointer-call class: user function pointer arguments now lower `&lvalue` to the lvalue handle, skip pointer lvalue access-chain materialization in call emit ranges, preserve pointer formatting in call emitters, and validate addressable value handles against pointer pointee types. The ad hoc `bevy-pbr-point-light` trace no longer drifts on the extra `AddressOf` / `_e46` vs `_e47` class, but now exposes a later expression/statement ordering drift around `enable_diffuse`. Official representative trace remains `20/20`; external compose remains `writer-drift=0 byte-drift=55`, with byte drift hashes refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next additive-product arena ordering class exposed by `point_light`: when an additive parent has multiplicative binary subtrees on both sides and neither side contains calls, the lowerer now preserves source-order subtree allocation instead of pre-lowering the higher-precedence right subtree. This aligns `diffuse * derived_input.NdotL + specular_light * specular_derived_input.NdotL` with upstream. External compose still has `byte-drift=55`; refreshed hashes now show the representative `point_light` first drift moved to later return temporary numbering (`_e84/_e86` vs `_e83/_e85`).
- 2026-06-04: Fixed the next binary-chain root predeclare class: pure left-associative binary chains now predeclare mutable source-local reference roots in source order before lowering loads and binary expressions. This aligns the minimal upstream trace for `lhs * factor * rhs` and removes the expression-order drift in the ad hoc `bevy-pbr-point-light` trace. External compose still has `writer-drift=0 byte-drift=55`; only the existing byte-drift hashes changed and were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next constructor/subtract-chain arena classes exposed by `spot_light`: constructor arguments with literal holes now lower non-literal access arguments before delayed literals while preserving component handles, and `literal - product - product` chains now append the higher-precedence product before the literal and keep the left subtract chain before the later product. The ad hoc `bevy-array-texture-spot-light` trace now has no expression-order drift; only a bindings normalization diff remains in that focused trace. External compose still has `writer-drift=0 byte-drift=55`, with `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `point_light` to `directional_light` generated temp numbering (`_e42` vs `_e41`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next compound-assignment RHS reference-root ordering class exposed by `directional_light`: compound assignments whose RHS mixes member access with source-local identifiers now predeclare RHS source-local roots before target-value loads. This aligns `color *= (*light).color.rgb * texture_sample` without applying the rule to plain arithmetic cases such as `mix_oklch`. External compose still has `writer-drift=0 byte-drift=55`, with `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `directional_light` to `search_for_blockers_in_shadow_map` generated temp numbering (`_e44` vs `_e35`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `search_for_blockers_in_shadow_map` drift class: function lowering now inlines static elements of global const arrays into the function arena, so `D3D_SAMPLE_POINT_POSITIONS[N]` lowers like upstream as fresh literal/compose expressions instead of `Constant + AccessIndex`. Compound assignment RHS predeclaration was also narrowed to root-call argument roots, aligning `sum += search_for_blockers_in_shadow_map_hardware(...)` without regressing `flags |= u32(...) * CONST` cases. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `search_for_blockers_in_shadow_map` to `random_rotation_matrix` generated temp numbering (`_e4` vs `_e5`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `random_rotation_matrix` drift class: user-call argument lowering now prelowers later nested value calls such as `select(...)` before earlier direct function-argument handles, foldable constant multiply chains lower after reference-producing calls, and matrix scalar constructors lower into Naga-style column vector compose expressions instead of relying on emitter expansion. Added `bevy-shadow-random-rotation` to the representative Naga writer trace manifest. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `random_rotation_matrix` to `fetch_point_shadow` generated temp numbering (`_e18` vs `_e17`). `shadow_sampling.wgsl` first drift is now `sample_shadow_map_castano_thirteen` temp numbering (`_e79` vs `_e78`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `fetch_point_shadow` arena ordering class: left-associative multiply chains whose right operand is a function-argument-root projection now predeclare that function argument root before lowering the left member load and named-expression product. This aligns `(*light).shadow_normal_bias * distance_to_light * surface_normal.xyz` with upstream. A focused `fetch_point_shadow` trace now has expression-order and binding parity; its remaining statement diff is a trace-normalization limitation because generated WGSL matches the oracle branch-local `_e60/_e62` placement. Official representative trace remains `21/21`. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `fetch_point_shadow` to `fetch_spot_shadow` generated temp numbering (`_e52` vs `_e56`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `fetch_spot_shadow` arena ordering class without regressing `fetch_point_shadow`: math-only additive left subtrees now stay before right products, literal-left division lowers denominator products before the delayed literal numerator, scalar conversion constructors lower as `As` expressions, return materialization treats `As` like constructor output, and later reference call arguments may only jump over prior alias/direct arguments without crossing earlier compound arguments. Focused `fetch_point_shadow` and `fetch_spot_shadow` traces now have expression-order and binding parity; their remaining local/statement diffs are trace-view limitations rather than the byte-level first blocker. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced from `fetch_spot_shadow` to `get_cascade_index` generated temp/local type ordering (`_e13: u32` vs `_e16: f32`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `get_cascade_index` dynamic access index materialization class: pointer-root dynamic member access now preserves static-member projection before local index lowering, access indexes materialize mutable local `var` values, and control-flow condition materialization now recurses through `Load(pointer)` into pointer subexpressions. This aligns `(*light).cascades[i].far_bound` with upstream as `let _e13: u32 = i; ... cascades[_e13].far_bound`. Added `bevy-shadow-get-cascade` to the representative trace manifest and a direct naga-oil writer regression for mutable local dynamic access indexes in conditions. External compose still has `writer-drift=0 byte-drift=55`; `array_texture.wgsl`, `shadows.wgsl`, and composed `pbr.wgsl` first drift advanced from `get_cascade_index` to `world_to_directional_light_local` bool temporary numbering (`_e32` vs `_e37`). Drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-04: Fixed the next `world_to_directional_light_local` short-circuit control-flow class: `if` / `else-if` / `while` conditions now enter node-driven short-circuit materialization, short-circuit result vars carry structured provenance, and generated bool result loads materialize through the normal temporary-name table instead of source-level writeback. Added `bevy-shadow-world-local` to the representative trace manifest. External compose remains `writer-drift=0` and improves to `byte-drift=54`; `ui_texture_slice.wgsl` is no longer a known byte drift, and `array_texture.wgsl` / composed `pbr.wgsl` first drift advanced to `sample_directional_cascade` generated temp numbering (`_e9` vs `_e8`). Drift manifests were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-05: Fixed the `sample_directional_cascade` trace class: left-associative binary chains now predeclare function-argument-root projections before delayed member loads, direct-access call arguments now jump only at a direct function-argument boundary instead of through a whole-call pre-pass, and nested control-flow statements now contribute their maximum expression index to the parent block emit watermark. Added `bevy-shadow-sample-directional-cascade` to the representative trace manifest. Representative trace is now `24/24`; external compose remains `writer-drift=0 byte-drift=54`, with byte drift hashes refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-05: Fixed the remaining `fetch_spot_shadow` PCSS argument-order class: direct-argument boundary lookahead now crosses inline named constants such as `SPOT_SHADOW_TEXEL_SIZE` without crossing literal values, preserving downsample entry ordering while matching upstream shadow call argument allocation. Added `bevy-shadow-fetch-spot` to the representative trace manifest. Representative trace is now `25/25`; external compose remains `writer-drift=0` and improves to `byte-drift=53`, with `decal/forward_decal.wgsl` removed from the byte drift taxonomy. `array_texture.wgsl` and composed `pbr.wgsl` first drift advanced to `prepare_world_normal` generated temp numbering (`_e9` vs `_e16`).
- 2026-06-05: Fixed the short-circuit generated temporary ownership class exposed by `prepare_world_normal`: short-circuit result loads now materialize through lowering at concrete use sites, the writer tracks already emitted materialized handles, and the representative trace normalizer derives `_eN` materialized temps from oracle WGSL instead of silently missing writer-emitted lets. Trace raw statements now skip only `Emit` rows owned by a traced short-circuit generated temporary declaration, avoiding duplicate `Emit(9)` / `Emit(22)` rows without suppressing ordinary generated temporaries. `bash tools/check_naga_writer_representative_trace.sh` is now `27/27`, the Bevy `oit_resolve.wgsl` `_e38` validation regression is covered by direct Naga fingerprinting, and external compose improves to `byte-exact=96 byte-drift=53` with `gradient.wgsl` and `ui.wgsl` removed from the byte drift taxonomy.
- 2026-06-05: Fixed the nested source-var initializer placement class exposed by `apply_pbr_lighting`: nested `var shadow: f32 = 1.0` now emits a hoisted declaration plus an execution-site store instead of a function-entry initializer, while `for` initializer variables still keep declaration initializers. Naga-compatible local declaration ordering now follows statement preorder first-declare order instead of raw local arena append order. `array_texture.wgsl` first drift advanced from local declaration/initializer order to generated temp numbering in `apply_pbr_lighting` (`_e14` vs `_e19`), and composed `pbr.wgsl` first drift advanced to the separate `diffuse_occlusion` initializer-placement class. External compose remains `writer-drift=0 byte-drift=53`; drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-05: Refined the initializer placement model from block depth to loop depth. Non-loop nested source vars such as `pbr_input_from_standard_material`'s `diffuse_occlusion` and `specular_occlusion` now keep declaration initializers like upstream, while loop-body vars such as `shadow_1` / `shadow_2` still lower to execution-site stores. Focused PBR output now matches upstream through those declarations, and the first composed `pbr.wgsl` drift advanced to generated temp numbering (`_e7` vs `_e8`). External compose still has `writer-drift=0 byte-drift=53`; drift hashes were refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`.
- 2026-06-05: Fixed the source-local const initializer literal caching class. Predeclared `var` const initializers now lower with the inferred declaration type even when no explicit type annotation is present, and literal cache entries are keyed by the literal's contextual scalar type rather than the whole initializer type. This aligns upstream reuse for cases such as `vec3<f32>(1.0)` followed by `f32 = 1.0` and keeps the `rgb_to_hsv_trace` representative at trace parity. Representative trace remains `27/27`; external compose still has `writer-drift=0 byte-drift=53`, with changed byte drift hashes refreshed from `_build/parity/external_naga_oil_compose/byte_failures.tsv`. `pbr.wgsl` first drift advanced from early `_e7/_e8` to later `_e33/_e32` temp numbering.

## Close Notes
