# ISS-043: Integrate moon_wesl behind the WGSL Core boundary

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: architecture, area/docs, area/testing, migration, release, wesl, agent
- Assignee: agent
- Created: 2026-07-20
- Updated: 2026-07-20
- External ref: https://github.com/moonbit-community/moon_wesl/commit/08fd313a638daa9b74245a9da5e3a51845a4fe40

## Description
Move the maintained `Milky2018/moon_wesl` implementation into this workspace,
then replace its duplicate official WGSL frontend semantics with the
`Milky2018/wgsl` boundary before publishing from this repository and
deprecating the former repository.

## Design
Import only the source repository's Git-tracked tree into
`modules/moon_wesl`. Preserve behavior in the first stage and keep WESL outside
the existing synchronized compatibility release line. In the second stage,
make WESL depend on WGSL Core for official WGSL parsing and validation while it
continues to own WESL syntax, module resolution, conditional compilation, and
assembly. Review and shrink the public API before the first release from this
workspace.

The imported `README.md` symlink is a temporary snapshot-preservation exception.
It must be replaced or deliberately supported before this issue closes.

## Acceptance Criteria
- [x] The Git-tracked tree from source commit `08fd313a638daa9b74245a9da5e3a51845a4fe40` is imported under `modules/moon_wesl` without repository or build state.
- [x] Module-local and workspace CI gates cover `moon_wesl` on every supported target.
- [x] `moon_wesl` depends on `Milky2018/wgsl` and does not publicly implement official WGSL validation.
- [x] Public WESL types and entry points have explicit owners; implementation packages remain internal.
- [x] Repository metadata and migration documentation point to this workspace without breaking the old package before replacement publication.
- [x] The next semantic version is selected from the public API diff and all release gates pass.
- [ ] A replacement package version is published from this repository before the former repository is deprecated.
- [x] All ISS-043 architecture exceptions are removed and the workspace gates pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-019, ISS-026, ISS-027, ISS-034
- Discovered from: none

## Notes
- 2026-07-20: Claimed for staged integration. Stage one is a behavior-preserving snapshot import; commit and publication are not implied.
- 2026-07-20: Stage one passes 181 WESL/CLI tests per backend and 1,046 workspace tests per backend. Workspace dependency-cache discovery and current-toolchain warning fixes are the only source adaptations after the verified snapshot import.
- 2026-07-20: Stage two routes assembled WGSL and CLI WGSL checks through an internal `Milky2018/wgsl/ir` adapter, removes the public `validate_wgsl` implementation, and moves matrix scalar legality into WGSL Core. WESL continues to own extension syntax, module resolution, cycle checks, lowering, and assembly.
- 2026-07-20: Release preparation selects 0.2.0 because the 0.1.2 public API diff removes `validate_wgsl`. The package repository now points to `moonbit-community/moon_wgsl`, the root public API is pinned by architecture metadata, and the imported README symlink is removed in favor of the workspace convention of referencing `README.mbt.md` directly.
- 2026-07-20: WGSL Core and its synchronized compatibility modules advance from 0.16.0 to 0.16.1 because matrix scalar validation is a public-API-compatible correctness fix. WESL 0.2.0 pins WGSL Core 0.16.1, and `publish.mbtx` accepts the two semantic versions independently while preserving dependency order.
- 2026-07-20: Release archive testing exposed a workspace-layout leak in the WGSL parser pre-build command. WGSL now searches ancestor dependency caches like WESL, and `tools/check_release_archives.sh` packages all five modules and tests extracted WGSL/WESL sources together. The archive gate passes 450 tests per backend; the source workspace passes 1,050 tests per backend plus strict checks, architecture guardrails, documentation freshness, and package integrity.
- 2026-07-20: The untruncated native diagnostic initially contained 1,979 unnecessary annotations across WESL, its grammar-generated parsers, and the WGSL validation dependency path; the earlier 141 figure was only the displayed diagnostic tail. Fixed-point checking exposed 10 more sites in WESL, Naga, and naga-oil. All 1,989 sites are addressed at their source, including 11 `.mbty` grammars, and the generated parsers are refreshed. Four empty marker-struct constructors retain their explicit type names under declaration-local `unnecessary_annotation` suppression because contextual `::{}` otherwise triggers the contradictory `struct_never_constructed` lint. Warning 73 is now enforced by module CI, workspace CI, extracted release archives, and `publish.mbtx`; the complete workspace passes `--target all --warn-list +73 --deny-warn`.
- 2026-07-20: Parser generation is updated from `moonbitlang/yacc` 0.7.13 to 0.7.17. The new release is wasm-only and cannot be installed as a default wasm-gc binary dependency by the current toolchain, so WGSL, WESL, and naga-oil define module-level `rule` declarations that invoke the pinned generator with `moon runwasm`; parser packages use `dev_build` without shell wrappers or physical dependency-cache paths. Regeneration is byte-stable after formatting, and this removes the old yacc source warnings from extracted-package checks.

## Close Notes
