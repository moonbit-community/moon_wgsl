# ISS-008: Introduce NagaCompatFunctionPlan as the sole function lowering contract

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: area/ir, architecture, lowering, agent
- Assignee: unassigned
- Created: 2026-06-05
- Updated: 2026-06-05
- External ref: WGSL-283

## Description
Function lowering currently appends expressions, materializes temporaries, and pushes statement emit ranges as interleaved side effects. This makes it possible to fix every drift with another local predicate. A future agent needs a single contract that describes the whole Naga-compatible function body before writer emission.

## Design
Add `NagaCompatFunctionPlan` as the structured output of function lowering. The plan should contain expression arena allocation, call argument schedule, short-circuit locals, materialized expressions, body emission order, local declaration order, and generated name slots. Existing lowerer paths may populate the plan during migration, but downstream writer logic must move toward consuming the plan.

## Acceptance Criteria
- [x] `NagaCompatFunctionPlan` exists as a concrete type with explicit fields for arena, call, short-circuit, materialization, body, and name planning.
- [x] At least one representative function is emitted through the plan without using ad hoc lowerer state.
- [x] The plan can be dumped in tests for stable golden comparison.
- [x] No public API promises are made around the transitional internal shape.

## Relationships
- Depends on: none
- Parent: ISS-007
- Related: ISS-003, ISS-006
- Discovered from: ISS-005

## Notes
- 2026-06-05: This is the first blocking task for the architecture refactor.
- 2026-06-05: Implemented the internal plan as `NagaCompatFunctionPlan`. Writer function and entry-point slots now carry the function plan, and emitter/name allocation/trace paths read the body plan through that contract.

## Close Notes
- 2026-06-05: Closed after adding `ir/wgsl_naga_compat_function_plan.mbt`, moving writer slots from `body_plan` to `function_plan`, adding a plan summary trace row, and adding a white-box test for stable plan dump sections.
- Verification: `moon check ir --deny-warn`; `moon test ir --filter 'Naga compat function plan dump exposes unified planning sections'`; `tools/check_architecture_guardrails.sh`; `bash tools/check_naga_writer_representative_trace.sh`; `moon test ir`; `moon check --target all --deny-warn`; `moon fmt`.
