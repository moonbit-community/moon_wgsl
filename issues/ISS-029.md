# ISS-029: Split overloaded WGSL lowerer into explicit planner layers

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: area/wgsl-core, area/ir, area/lowering, architecture, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: none

## Description
The WGSL lowerer still concentrates too many semantics in a small set of very large files. `modules/wgsl/ir/wgsl_lower_function_expressions.mbt` and `modules/wgsl/ir/wgsl_lower_statements.mbt` own expression lowering, constant folding, call argument planning, materialization, statement emission order, and several trace-derived Naga compatibility ordering fixes.

This makes future parser, IR, and writer changes risky because local expression fixes can silently change arena order, temporary names, or statement output far away from the modified branch.

## Design
Replace the overloaded lowerer shape with explicit planner layers that expose small contracts:

- module and global predeclaration planning
- type lowering
- function signature and body lowering
- statement planning
- expression arena scheduling
- const evaluation
- call and builtin planning
- side-effect materialization
- temporary naming provenance

Trace-derived Naga compatibility behavior must be represented as planner invariants or deleted by a unified model, not embedded as long case-specific helper names inside expression lowering.

## Acceptance Criteria
- [x] `wgsl_lower_function_expressions.mbt` is split so expression lowering, expression arena scheduling, call planning, const evaluation, and materialization each have one clear owning package/file.
- [x] `wgsl_lower_statements.mbt` is split so control-flow lowering and body emission planning are separate responsibilities.
- [x] Trace-derived helpers such as `lower_function_binary_const_parent_between_right_const_and_local_reference` are removed or replaced by named planner rules with tests that describe the invariant rather than the fixture.
- [x] Statement emission uses explicit body plans and no longer asks expression lowering code to make final writer-order decisions.
- [x] `moon check --target all --deny-warn`, `moon test --target all`, `bash tools/check_naga_writer_representative_trace.sh`, and `bash tools/check_external_naga_oil_compose_parity.sh` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-008, ISS-009, ISS-010, ISS-011, ISS-012, ISS-014
- Discovered from: current code-quality review

## Notes
- 2026-07-01: Current largest non-generated source files include `modules/wgsl/ir/wgsl_lower_function_expressions.mbt` and `modules/wgsl/ir/wgsl_lower_statements.mbt`; both remain structural hotspots after the workspace split.

## Close Notes
Closed on 2026-07-01. Split the overloaded lowerer files into focused planner files: binary expression planning, named call dispatch, constructor argument planning, math call planning, relational/select planning, atomic call planning, expression shape helpers, source-local const planning, statement control-flow planning, statement local analysis, and statement expression policy. `wgsl_lower_function_expressions.mbt` dropped from 6431 lines to about 1265 lines, and `wgsl_lower_statements.mbt` dropped from 3865 lines to about 1340 lines. Renamed the old `lower_function_binary_*` trace-derived branch helpers to `plan_binary_*` planner-rule entry points. Verification: `moon check --target all`, `moon -C modules/wgsl check --target all --deny-warn`, `moon test --target all`, `bash tools/check_naga_writer_representative_trace.sh` passed 45 cases, and `bash tools/check_external_naga_oil_compose_parity.sh` passed `cases=170 comparable=149 oracle-blocked=21 writer-exact=149 byte-exact=149`. Root `moon check --target all --deny-warn` remains blocked by vendored `.mooncakes/moonbitlang/yacc` warnings outside this module, so the issue-specific deny-warn proof is module-local.
