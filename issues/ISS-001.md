# ISS-001: Prove upstream execution model parity

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: area/parity, area/naga-writer, agent
- Assignee: unassigned
- Created: 2026-06-03
- Updated: 2026-06-03
- External ref: WGSL-283

## Description
The current repository cannot honestly claim that the Moon WGSL execution model is identical to upstream Naga or naga-oil. Recent validation proves important slices: representative writer trace parity passes for 20 cases, external naga-oil compose parity has `writer-drift=0`, and the Bevy corpus composes without current validation failures. However, strict byte parity still reports `byte-drift=55`, and existing trace gates compare only selected expression order, bindings, and materialized temporaries.

This epic tracks the work required to turn execution-model parity from an assumption into a checked property.

## Design
Do not solve this by editing individual expected strings or case-specific writer policy. Build upstream-comparable execution traces for each stage, then align the internal model where traces diverge. The tracked stages are module/declaration arena construction, function body lowering and statement plan, materialization and temp-name planning, and final byte writer fragments.

## Acceptance Criteria
- [x] The tracker contains one issue per unproven execution stage.
- [x] A future agent can run gates that compare upstream and Moon execution traces, not only final text.
- [x] Remaining byte drift cases are classified by trace-derived root cause, not by filename.
- [x] The project can state exactly which execution stages are proven equivalent and which are not.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-06-03: Current evidence is insufficient for a full equivalence claim. `writer-drift=0` is necessary but not sufficient; `byte-drift=55` proves final behavior is not fully byte-compatible.
- 2026-06-05: Execution-model parity is now tracked by dedicated stage issues. Module arena tracing, body execution tracing, byte-fragment tracing, unified Naga-compatible function planning, and import-retention construction are represented by closed child issues. Remaining byte output incompatibility is explicitly not closed here; it is tracked by ISS-005 and final strict parity by ISS-015.

## Close Notes
Closed as the evidence/tracking epic, not as a claim that byte output is already identical. Current proven slices: representative Naga writer trace `28/28`, external compose writer/order/name parity `writer-drift=0`, compose pipeline-interface subset fingerprinting in the external gate, and trace-derived byte-drift taxonomy. Current unproven slice: exact byte output, still `byte-drift=52`, tracked by ISS-005 and ISS-015. Verification evidence from the latest completed gates: `moon test` (747/747), `moon check --target all --deny-warn`, `bash tools/check_architecture_guardrails.sh`, `bash tools/check_naga_writer_representative_trace.sh` (28/28), `bash tools/check_external_naga_oil_drift_taxonomy.sh` (`writer-drift=0 byte-drift=52`), and `MOON_WGSL_ALLOW_KNOWN_DRIFT=1 bash tools/check_external_naga_oil_compose_parity.sh` (`cases=150 comparable=149 oracle-blocked=1 writer-exact=149 writer-drift=0 byte-exact=97 byte-drift=52`).
