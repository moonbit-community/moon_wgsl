# ISS-017: Add byte-level diagnostic parity for compose and preprocess failures

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/diagnostics, area/parity, area/preprocess, area/compose, agent
- Assignee: unassigned
- Created: 2026-06-05
- Updated: 2026-06-05
- External ref: WGSL-284

## Description
Successful compose/preprocess output parity does not prove failure behavior parity. The legacy tracker recorded that compose/preprocess errors are not byte-gated against the pinned `naga_oil` oracle across the external compose corpus, so diagnostic category, message, source location, or byte formatting can drift unnoticed.

This issue is migrated from the legacy tracker row `WGSL-284`.

## Design
Use `bevyengine/naga_oil@bc444c82bb593ede94c55cdbf799e9743800843e` as the sole oracle for negative cases. Add manifest-owned negative compose/preprocess fixtures and compare machine-readable diagnostics plus final diagnostic bytes. The gate should reject unchecked diagnostic drift by default.

## Acceptance Criteria
- [x] Negative compose/preprocess cases are manifest-owned and schema-checked.
- [x] Moon and oracle diagnostics are compared for category, message, source span/location, and rendered bytes.
- [x] Diagnostic drift fails CI by default, with no passive allowlist mode for unchecked failures.
- [x] Existing success-path parity gates remain independent from diagnostic parity gates.

## Relationships
- Depends on: ISS-001
- Parent: none
- Related: ISS-015, ISS-016
- Discovered from: WGSL-284

## Notes
- 2026-06-05: Migrated from `docs/moon_wgsl-issue-tracker.md`. The old row was `TODO`.
- 2026-06-05: Added `testdata/naga_oil_upstream/compose_tests/error_parity_cases.tsv` as the negative diagnostic manifest. `tools/check_moon_wgsl_error_parity.sh` now runs the pinned naga-oil oracle and moon_wgsl for every manifest row, compares machine-readable diagnostic summaries (`category`, `message`, `location`, `bytes_sha256`), then compares full rendered diagnostic bytes.

## Close Notes
Closed after converting the error parity gate from hand-written static expected-file checks to live manifest-driven oracle comparison. CI already invokes `bash tools/check_moon_wgsl_error_parity.sh`, and architecture guardrails now require the diagnostic manifest, schema checks, oracle comparison, summary diff, and byte diff. Verification passed: `bash tools/check_moon_wgsl_error_parity.sh` (`cases=7`), `bash tools/check_naga_oil_parity_inventory.sh`, `bash tools/check_architecture_guardrails.sh`, `moon test` (747/747), and `moon check --target all --deny-warn`.
