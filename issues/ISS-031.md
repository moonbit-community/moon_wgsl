# ISS-031: Make compose reference liveness purely symbol-graph driven

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: area/compose, area/naga-oil, area/symbol-graph, architecture, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: none

## Description
The naga-oil composer is symbol-binding-first, but reference liveness still has source-assisted semantic scans. `WgslComposeSemanticFacts` and import request planning still consult source strings for unqualified reference names, reference paths, and colon-pair context.

These scans are not the old raw text replacement path, but they still mean the Symbol Graph is not the only source of semantic truth.

## Design
Make parsed and bound reference facts the sole input for alias/import/name reachability. Source text may be preserved for diagnostics and final source-map material, but compose liveness, import retention, alias redirects, and qualified reference decisions must use structured facts derived from the parser/AST and symbol binding.

## Acceptance Criteria
- [x] `wgsl_semantic_source_contains_unqualified_reference_name`, `wgsl_semantic_source_contains_reference_path`, and colon-pair source helpers are removed from compose liveness.
- [x] `active_wgsl_full_module_import_dependency_scan_source` is replaced by structured bound reference facts.
- [x] Import request filtering reads `WgslComposeSemanticFacts` or Symbol Graph data, not raw source snippets.
- [x] Source Edit Backend consumes an already-bound `EmissionPlan` and does not decide semantic reachability.
- [x] External naga-oil compose parity passes with `cases=170` or a documented higher current count, and no new source-level liveness fallback is introduced.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-023, ISS-024, ISS-015, ISS-016
- Discovered from: current code-quality review

## Notes
- 2026-07-01: The current guardrails reject source-level writeback, but compose still passes source strings into semantic reference scans. This issue tracks eliminating that remaining semantic dependency.
- 2026-07-01: Closed by replacing compose liveness with `WgslComposeSemanticFacts` queries. Full-module dependency scan now returns merged reference facts instead of appending extracted source, unqualified import filtering reads facts, and alias redirect match counting parses facts only when redirects exist so missing-import diagnostics keep upstream priority. Verified with no grep hits for the removed source-liveness helpers, module check/test, architecture guardrails, and external naga-oil compose strict byte parity (`cases=170 comparable=149 oracle-blocked=21 writer-exact=149 byte-exact=149`).
