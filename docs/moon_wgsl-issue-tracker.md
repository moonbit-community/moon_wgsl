# moon_wgsl Issue Tracker

Last updated: 2026-05-18

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| `WGSL-284` | Naga-oil diagnostic byte parity | Compose/preprocess errors are not yet byte-gated against the pinned `naga_oil` oracle across the external compose corpus, so output success parity can improve while failure behavior still drifts. | `TODO` | Use `bevyengine/naga_oil@bc444c82bb593ede94c55cdbf799e9743800843e` as the sole oracle. Add manifest-owned negative compose/preprocess cases, compare diagnostic category/message/source location bytes, and reject unchecked diagnostic drift by default. |
| `WGSL-283` | Naga WGSL writer policy clone | The current IR emitter still applies moon_wgsl-owned expression materialization, temporary naming, numeric spelling, and statement formatting decisions instead of cloning the pinned Naga writer behavior that `naga_oil` uses. | `IN_PROGRESS` | Local name conflict suffixing follows Naga's per-base collision numbering instead of arena handle indices, value indexing no longer treats immutable `let` bases as lvalues, F32 and concrete abstract-float emission now uses shortest f32 round-trip spelling, numeric unary negation emits token-style `-1f`, scalar const local `let` bindings lower through `local_constants`, typed integer and typed float global const expressions fold under the naga-oil writer policy, scalar-vector `+`/`-`/`/`/`%` lower through explicit `vecN(scalar)` splats like Naga IR, and scalar matrix constructors expand into Naga-style column-vector constructors. Materialization recursion now covers Math, Select, Derivative, Relational, As, texture/image query value arguments, subgroup calls, cooperative expressions, store-value function calls, mutable member/index loads before stores, and short-circuit `&&` / `||` return-value use. Runtime `var` initializers now split into declaration plus store while const `var` initializers remain inline, matching the pinned Naga writer shape for Bevy gradient-style code. Short-circuit expressions now lower structurally into Naga-style local result variables plus branch control flow instead of being emitted as direct WGSL boolean chains. The emitter now builds a single Naga-style name table in pinned reset order (types/members, entry points, functions, globals, constants, overrides), and declarations/references for types, struct members, constants, overrides, globals, functions, arguments, and locals all read from that table instead of ad-hoc suffix helpers. Generated expression temporaries now use Naga's per-function `_eN` namespace instead of the module-global collision namespace, while user locals/arguments still collide globally like Naga. User function calls that need a value now lower to `Statement::Call` plus `CallResult` instead of a source-shaped `FunctionCall` expression, function arguments reuse stable expression handles, and ambient-global call analysis scans both expression calls and statement calls. The naga-oil writer profile now follows Naga `EXPLICIT_TYPES` for concrete local declarations, local `let` storage types are concretized from abstract numerics before emission so Bevy `gradient.wgsl` no longer drifts on `let nearest_border: f32` / `let t: f32`, texture mip-level arguments are lowered with i32 context, and homogeneous `min`/`max`/`clamp` math calls now context-fill abstract literal arguments from concrete sibling argument types. This round added explicit generated-temporary provenance on IR local variables, keeps immutable `let` references as value expressions instead of load expressions, folds unary negative numeric literals before IR insertion, skips immutable source `let` references when allocating Naga-style `_eN` expression names, and preserves direct non-side-effecting texture/image value-call `let` initializers plus direct immutable-let returns. It also tracks Naga-style projected temporary expression slots when materialized value calls are immediately accessed, and the emitter now carries a per-function baked-expression table so materialized child handles print through their generated `_eN` names without forcing parent expression handles to be appended. Scalar float constructors over literal and constant integer values now fold through the naga-oil writer policy, matching Naga output for `f32(0xFFFFFFu)` / `f32(0xFFu)`. Call-result temporary naming now uses expression-graph reachability to distinguish materialized call arguments from ordinary let-initializer argument references, which removes Bevy `sprite.wgsl` from the byte-drift manifest without regressing `single_mip_level.wgsl`, `occlusion_culling.wgsl`, or projected `textureLoad` numbering. Generated temporary locals now carry store-value usage provenance so simple assignment RHS materialization no longer consumes an extra Naga expression slot, removing Bevy `ui_material.wgsl` from the byte-drift manifest without regressing nested call arguments or projected texture loads. Self-contained nested function imports are now retained for selected item modules while modules with their own import graph stay guarded by the existing IR dependency path, removing Bevy `custom_material_screenspace_texture.wgsl` from the writer-drift manifest without reintroducing extra resource bindings in `array_texture.wgsl`. Final-name planning now carries the import request module identity instead of re-deriving provenance from `rel_path`, so quoted file imports decorate with the upstream raw quoted path; this removes Bevy `custom_material.wgsl` and `custom_material_2d.wgsl` from both writer and byte drift manifests. Selected-item import slices now prune unreferenced explicit const/function imports through AST reference paths while retaining type/alias dependencies, removing Bevy `tonemapping.wgsl` and one additional selected-import writer drift without regressing resource binding retention. The Naga-style name table now allocates module-level and function-signature names before function-body locals, so cross-function argument/local collisions follow writer phase order instead of per-function eager allocation. Control-flow lowering now materializes user-call, atomic-call, workgroup-uniform-load, short-circuit boolean conditions, and nested global binding access operands into Naga-style temporaries for `if`, `else if`, `while`, and `for` guards without materializing ordinary local-variable loads; `while` materialization happens inside the loop body guard rather than outside the loop, preserving per-iteration evaluation. AccessIndex emission is now type-driven: vectors emit swizzles, structs emit member names, and arrays/matrices/pointers emit bracketed indices, removing the previous blanket bracket policy that regressed vector projections. Abstract vector constructors now consume concrete sibling/outer numeric context only while their own type is still abstract; shift counts and already-concrete nested conversion constructors keep their original typed provenance. Function lowering now installs the declared return type before lowering the body, so return expressions receive result-type context and abstract return constructors emit Naga-style concrete literal spelling. The writer also folds constant `sqrt` math calls through the same numeric constant policy, matching Naga.s `box_shadow.wgsl` spelling for `sqrt(2. * PI)` and `sqrt(0.5)`. Function emission now hoists all non-generated local `var` declarations, including const-initialized vars and `for` initializer vars, before local `let` statements like Naga writer; local variable initializers are contextualized after type inference so inferred `i32` loop counters emit `0i` instead of abstract `0`; Naga-style name allocation now reserves WGSL builtin function names, so locals such as `step` are renamed to `step_`; `for` bodies now preserve the Naga loop-body block scope, and update expressions materialize their loaded target before storing. IR functions and entry points now retain source positions, and Naga-compatible function ordering preserves non-generated helpers that appear before each entry point before applying reachability/dependency expansion, removing Bevy `ui.wgsl` and one additional writer-order drift without regressing `gradient.wgsl`, `sprite_material.wgsl`, or mip downsample cases. Pure helper modules without entry points now emit fallback functions through dependency-before-caller traversal while entry-point reachability keeps Naga's source-prefix behavior, removing additional helper-order drift such as `pbr_lighting.wgsl` without regressing `gradient.wgsl`. Struct type predeclaration now follows first type-reference position rather than raw source declaration order, matching Naga's placeholder allocation when function signatures forward-reference later struct declarations and removing Bevy `raymarch.wgsl` from writer drift. Abstract numeric global constants are now inline-only under the naga-oil writer policy, with contextual folding driven by concrete sibling/target scalar type instead of declaration names; this removes Bevy `rgb9e5.wgsl`, `pbr_deferred_types.wgsl`, and `pbr_prepass_functions.wgsl` from writer drift. Import request declared-dependency ordering now mutates the execution request queue instead of returning an ignored ordered copy, and IR struct predeclaration now topologically emits field type dependencies before owner structs; this removes Bevy `meshlet_bindings.wgsl` from writer drift. Current strict parity rejects 38 writer/order/name drifts and 95 byte-output drifts; the remaining drift is still dominated by declaration/import order, root/local final-name policy, helper/local declaration ordering, and Naga temporary/name allocation details. |
| `WGSL-282` | Imported module declaration retention | The strict external compose gate shows many cases where upstream emits declarations that are not directly reachable from the root entry point, while moon_wgsl prunes them. This means compose is still optimizing rather than cloning `naga_oil` module construction. | `IN_PROGRESS` | Split root/full import request construction from item-imported-module import construction. Item slices now use their extracted source for live requested-item/resource planning, and separately scan the prepared full active module only to supplement explicit alias imports with non-resource dependency items (`struct`, `type`, `const`, `override`, and self-contained `function`). Name-based unqualified binding fallbacks were removed. This fixes the `animate_shader` transitive alias type loss without reintroducing `array_texture` unused `raymarch` imports or `water_material` extra `lights`/`clustered_lights` resource bindings. Selected-item slices now AST-prune unreferenced explicit const/function imports while keeping struct/type dependencies, matching upstream for Bevy tonemapping selected imports. Resource side effects from selected-item full-module imports are now retained only when the prepared full module actually references a resource binding from a single-target unaliased import, preserving `prepass_bindings::previous_view_uniforms` without leaking grouped or aliased helper-module resources such as Bevy `raymarch`, `pbr_bindings`, or broad `mesh_view_bindings` bindings. Current strict gate reports 38 writer and 95 byte drifts, with pipeline-interface subset parity passing across the full external Bevy compose corpus; declaration retention/order is still not byte-complete. |
| `WGSL-281` | External compose byte-drift elimination | The full Bevy external compose corpus currently has 38 writer/order/name drifts and 95 full-byte drifts against pinned upstream. Drift manifests are no longer acceptable as proof of compatibility. | `TODO` | Import request planning now batches all explicit `#import` directives in a source before sorting by AST reference order, and the naga-oil writer policy now folds typed integer constants plus expands scalar matrix constructors. Burn the remaining drift down to zero under the default strict gate. Keep `MOON_WGSL_ALLOW_KNOWN_DRIFT=1` only as a temporary legacy investigation mode until this row is done, then remove the legacy drift manifests and mode. |
| `WGSL-280` | Strict external naga-oil byte parity gate | The external compose parity gate accepted known writer/byte drift manifests by default, so CI could remain green while byte-level incompatibility was known. | `DONE` | Default mode now fails on any writer fingerprint or byte-output drift, writes machine-readable reports plus per-case diffs under `_build/parity/external_naga_oil_compose`, and requires explicit `MOON_WGSL_ALLOW_KNOWN_DRIFT=1` for the old drift-manifest compatibility check. Verified on 2026-05-14 with `bash -n tools/check_external_naga_oil_compose_parity.sh`, `moon check --deny-warn`, `tools/check_architecture_guardrails.sh`, and strict gate failure reports. Later WGSL-282 work intentionally changed observed drift hashes/counts, so the legacy drift manifests are no longer treated as a current compatibility source. |
| `WGSL-279` | Transform semantic-free rewrite backend | The first split moved rewrite plans and collectors out of `transform/wgsl_binding.mbt`, but transform still owned name-first rename-plan semantics used by virtual overrides and white-box rewrite helpers. | `DONE` | Deleted the internal `WgslRenamePlan` / `WgslRenameRule` / rename-map path and rewired virtual override rewriting to use structured `WgslReferenceRewritePlan` bindings plus body-only bound rewrite collection. Removed rename-plan white-box tests and kept binding-plan coverage for recursive references and keyword safety. Guardrails now reject reintroducing rename-plan/map helpers or name-first target lookup in transform. Verified with `moon check transform --deny-warn`, `moon test transform --target all`, `moon check compose ir transform --deny-warn`, and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-278` | Compose explicit stage objects | The first split deleted `compose/import_graph.mbt`, but compose still passed session/binding/source state through procedural functions rather than explicit `ImportGraphBuilder`, `ReachabilityPlan`, `FinalNameAllocator`, and `ComposeEmitter` objects. | `DONE` | Added explicit `WgslImportGraphBuilder`, `WgslReachabilityPlan`, `WgslFinalNameAllocator`, and `WgslComposeEmitter` stage objects. Moved graph planning/execution onto `WgslImportGraphBuilder`, source assembly/root emission onto `WgslComposeEmitter`, final live-binding computation through `WgslReachabilityPlan`, and import final-name application through `WgslFinalNameAllocator`. Guardrails now require these stage objects, require stage entrypoint usage, and reject reintroducing graph-planning/source-emission internals as `Composer` methods. Verified with `moon check compose ir --deny-warn` and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-277` | IR emitter writer policy architecture | The first split separated statement/expression emission, but module ordering, root filtering, local declaration policy, type/global/function emission, and valid-vs-naga-oil writer policy still shared one large emitter file. | `DONE` | Split emitter responsibilities into core entrypoint wiring, writer policy, module order/filtering, declarations, functions/local declaration policy, type spelling, attributes/literals, name allocation, builtin/operator spelling, expressions, and statements. Guardrails now require the split files, cap emitter core at 250 lines, cap non-test emitter split files at 1700 lines, and require writer policy ownership of `WgslIrEmitOptions`. Verified with `moon check ir --deny-warn` and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-276` | IR lowerer explicit sub-pipelines | The first split moved const eval, materialization, and statement lowering out of `ir/wgsl_lower.mbt`, but module orchestration, global declaration lowering, function lowering, expression lowering, builtin lowering, and provenance remained mixed around one mutable lowerer. | `DONE` | Deleted the old `ir/wgsl_lower.mbt` monolith and split the lowerer into core/module, globals, functions, updates, type inference, expression token parsing, global expressions, function expressions, call/builtin lowering, expression result typing, const eval, materialization, statements, and focused feature files. Guardrails now require these split files, reject reintroducing `ir/wgsl_lower.mbt`, and cap each non-test lowerer split file at 1500 lines. Verified with `moon check ir --deny-warn` and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-275` | Resolver architecture cleanup | Module path normalization policy is spread across resolver, compose, and oracle/tooling assumptions, including `assets/`, `shaders/`, file-path display, and import-module inference. | `DONE` | Centralized resolver-owned module-path inference/defaulting into `resolver/module_path_policy.mbt`, moving implicit rel-path module inference and module-path default rel-path conversion out of registry mutation and module-resolution execution files. Added architecture guardrails requiring the policy file and rejecting policy helpers in `registry_ops` / `module_resolution`. Verified with `moon check resolver --deny-warn` and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-274` | Directive/preprocess/metadata architecture cleanup | `directive`, `preprocess`, `metadata`, and compose still share directive scan source text and independently interpret cleaned/original source fragments. | `DONE` | Moved source-level WGSL directive item parsing out of `metadata` into the `directive` package, so metadata consumes directive-owned structured source directive items instead of owning a separate parser. Added architecture guardrails rejecting `metadata/source_directive_items*.mbt` and requiring `directive/source_directive_items.mbt`. Verified with `moon check directive metadata --deny-warn`, `moon test directive metadata --target all`, and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-273` | Common package architecture cleanup | `common` carries unrelated cross-stage records: diagnostics, shader defs, compose options, prepared source, source catalog, preprocess output, and export output. | `DONE` | Deleted the broad `common/types.mbt` bucket and split common DTO ownership into `shader_defs`, `import_types`, `directive_types`, `preprocess_types`, `source_types`, `diagnostic_types`, and `compose_export_types`. Added architecture guardrails requiring the domain files and rejecting reintroduction of `common/types.mbt`. Verified with `moon check common --deny-warn` and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-272` | AST package architecture cleanup | `ast` still exposes broad constructor/getter and identifier-collection APIs, so syntax storage and semantic fact collection remain coupled. | `DONE` | Moved semantic identifier/reference collection helpers out of `ast` into the new `ast_analysis` package, rewired IR lowering and parser white-box tests to depend on that package, and left `ast` with syntax data plus the basic `WgslGlobalDecl::name` accessor only. Added architecture guardrails preventing identifier collectors from returning to `ast`. Verified with `moon check ast ast_analysis ir parser --deny-warn`, `moon test parser ast_analysis ir --target all`, and `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-271` | Parser architecture cleanup | Parser architecture should not treat direct MoonYacc generated output as an API smell; generated tokens/start symbols are a build-tool consequence and do not need local visibility restrictions. | `DONE` | Kept direct `$mod_dir/.mooncakes/moonbitlang/yacc/moonyacc` pre-build generation for downstream build reliability, and removed the guardrails that attempted to whitelist or restrict MoonYacc-generated public symbols. The stable hand-written library entrypoint remains `parse_wgsl_translation_unit_strict`, but generated parser content is intentionally accepted as generated surface. Verified with `tools/check_architecture_guardrails.sh` on 2026-05-10. |
| `WGSL-270` | Transform architecture cleanup | `transform` still owns token replacement, canonical source emission, rename maps, and binding rewrite execution even though semantic rewrite policy now belongs to compose/IR. | `DONE` | First-stage split only: separated rewrite plan, collector, and facade files and capped facade size. Remaining semantic-free backend work is tracked by `WGSL-279`. |
| `WGSL-269` | Compose architecture cleanup | `compose` still mixes import graph construction, reachability, final-name allocation, virtual override handling, live binding planning, and source assembly in the same execution path. | `DONE` | First-stage split only: deleted `compose/import_graph.mbt` and split source preparation, request building, request execution, final-name planning, and finalization files. Remaining stage-object work is tracked by `WGSL-278`. |
| `WGSL-268` | IR emitter architecture cleanup | `ir/wgsl_emit.mbt` combines module ordering, writer policy, root filtering, statement emission, expression emission, and naga-oil-compatible output policy in one large emitter. | `DONE` | First-stage split only: separated statement and expression emission files and capped the remaining emitter monolith. Remaining writer-policy work is tracked by `WGSL-277`. |
| `WGSL-267` | IR lowerer architecture cleanup | `ir/wgsl_lower.mbt` is a near-8000-line monolith that owns module predeclaration, type lowering, function lowering, statement/expression lowering, const evaluation, materialization, builtins, and generated-import provenance. | `DONE` | First-stage split only: moved const evaluation, expression materialization, and statement/control-flow lowering out of the monolith. Remaining explicit lowerer pipeline work is tracked by `WGSL-276`. |
| `WGSL-266` | Upstream writer parity / byte upgrade | Real-project external compose parity currently compares Naga IR interface fingerprints, not byte-level writer output, so declaration formatting/order/name-allocation differences remain outside the strongest upstream parity gate. | `DONE` | Added manifest-owned byte-output drift gating for every oracle-comparable external naga-oil compose case. The gate now compares full moon_wgsl writer output against pinned upstream naga-oil output, records byte diffs by stable path-independent hash, and exact-gates the observed byte-output drifts while proving 25 comparable cases are byte-exact. Verified with `tools/check_external_naga_oil_compose_parity.sh` on 2026-05-10. |
| `WGSL-265` | Upstream writer parity / order-name fingerprint | The external compose parity fingerprint checks entry points and pipeline-facing interface subsets, but it does not yet record full writer-sensitive structure such as declaration sequence, generated final-name table, helper emission set, helper order, and temporary naming. | `DONE` | Added `wgsl_writer_fingerprint`, wired it into the external naga-oil compose parity gate, and exact-gated writer/order/name drift with stable path-independent diff hashes. The gate now proves 55 comparable cases match the pinned upstream writer/order/name fingerprint exactly and records the current drift cases in a manifest-owned table. Verified with `tools/check_external_naga_oil_compose_parity.sh` on 2026-05-10. |
| `WGSL-264` | External compose parity / full source inventory | The external naga-oil compose parity gate covers only 6 critical Bevy profiles, while the external corpus has 150 concrete Bevy source files that exercise naga-oil compose. This leaves broad real-project import/name-resolution behavior checked only by moon_wgsl validity, not by upstream differential parity. | `DONE` | Expanded external naga-oil compose parity to the full manifest-owned 150-case Bevy compose source inventory. The gate now exact-diffs the parity manifest against `external_wgsl_corpus_compose_sources.tsv`, applies the same profile overlays/raw template substitutions/capability facts as the external corpus gate, compares 149 oracle-comparable cases against pinned upstream, and records the one pinned upstream writer panic (`assets/shaders/custom_clustered_decal.wgsl`) in an explicit oracle-blocked manifest. Verified with `tools/check_external_naga_oil_compose_parity.sh` on 2026-05-10. |
| `WGSL-263` | External compose parity / scalable harness | The external naga-oil compose parity harness could not scale to the full compose source inventory because it rejected `raw:` template value definitions and re-fetched the same repository for every manifest row. | `DONE` | Added overlay materialization for raw template value definitions so both moon_wgsl and the pinned upstream oracle compose the same materialized source, cached consecutive repository checkouts inside the gate, and added architecture guardrails for both capabilities. A full 150-case trial now reaches a real pinned-oracle writer panic at Bevy `custom_clustered_decal.wgsl` instead of harness limitations; that full-inventory expansion is tracked by `WGSL-264`. Verified with `tools/check_external_naga_oil_compose_parity.sh`, `tools/check_architecture_guardrails.sh`, and `git diff --check` on 2026-05-10. |
| `WGSL-262` | Upstream compose parity inventory | Several mirrored upstream deterministic fixtures were still classified outside moon_wgsl local execution: GLSL/WGSL mixed compose outputs and selected Naga parser/validator diagnostics were checked only by the Rust oracle, so moon_wgsl could regress those compatibility surfaces without the inventory noticing. | `DONE` | Added `.glsl` fixture registration/lowering in the parity tool, made GLSL frontend module imports preserve whole-module emission, represented GLSL-driven imported const inlining in IR provenance, and generated source-level Naga-style diagnostics for the mirrored parser/validator failures. `tools/check_moon_wgsl_byte_parity.sh` now byte-gates all deterministic GLSL expected outputs, `tools/check_moon_wgsl_error_parity.sh` gates the selected Naga-style diagnostics, and the inventory no longer accepts moon gates that are executed only by the external oracle. The only remaining non-byte row is `diagnostic_filters`, which is explicitly semantic because the pinned upstream oracle currently fails validation for that should-panic fixture. Verified with full target check/test plus local byte/error/inventory/preprocess gates on 2026-05-09. |
| `WGSL-261` | Strict statement terminators | The statement collector treated a closing `}` as a valid terminator for simple statements, so invalid WGSL like `z = 1.0 }` could be accepted by moon_wgsl even though Naga rejects it with `expected ;`. | `DONE` | Statement collection now records whether a statement ended with a top-level semicolon, and strict AST validation marks semicolon-required statements invalid when the semicolon is missing. Template interpolation braces from naga_oil-style `#{...}` are tracked separately from WGSL block braces, so recursive block validation does not descend into source templates. Added parser regressions for assignment, return, and function-call statements without semicolons, and verified the real wgpu `issue_4485.wgsl` invalid source is now rejected before IR lowering. |
| `WGSL-260` | Composer semantic phase architecture | Even after composer rewrites became symbol-binding-first, several phase boundaries still compressed semantic objects into strings: semantic reference facts were `HashSet[String]`, compose bindings carried `from_name/to_name/identity?`, live binding resolution stored `resolved_to_name`, and transform rewrite bindings accepted string reference names. | `DONE` | Added structured semantic reference path sets, changed compose bindings to carry `WgslSemanticReferencePath` plus a non-optional `WgslComposeSymbolTarget`, changed live binding resolution to pass target symbol objects instead of resolved-name strings, and changed transform rewrite plans to accept `WgslReferencePath` rather than string bindings. Added architecture guardrails banning `add_symbol_binding`, optional identity compose bindings, string-only semantic reference sets, and resolved-name phase state. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-259` | Composer symbol-binding architecture | `WgslReferenceRewritePlan` carried stable symbol identities but still exposed binding rewrites by downgrading them into `WgslRenamePlan`; the final AST rewrite therefore remained name-first at the execution boundary, and unqualified duplicate import bindings could remain ambiguous until transform declined a rewrite. | `DONE` | Removed the binding-plan-to-rename-plan adapters, removed optional identity from plain rename rules, and made reference/declaration binding rewrites consume `WgslReferenceRewriteBinding` directly when collecting AST identifier nodes. Composer binding sets now reject same-scope/same-reference bindings that point at different identities, including unqualified imports. Added guardrails preventing `reference_rename_plan` / `global_declaration_rename_plan` from returning and documenting that composer rewrites must remain symbol-binding-first. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-258` | 100% WGSL/naga-oil quality gate | External real-project WGSL scanning still allowed skipped files to remain implicit, so coverage regressions could hide behind a stable aggregate skipped count. | `DONE` | Replaced skipped/expected-failure handling with `testdata/external_wgsl_corpus_expected_invalid.tsv`: the external corpus gate now reports `skipped=0` and `expected-failures=0`, while standalone-invalid source files are a separate negative corpus contract that must remain Naga-rejected. Added real Bevy sprite/UI render source roots plus concrete Bevy tonemapping, deferred, environment-map, SSR, UI, meshlet, and cluster-raster profiles; added texture-atomic/cooperative-matrix/64-bit-image-atomic/f64/mesh-shader/ray-hit-vertex-position/per-vertex/binding-array IR/oracle validation plus duplicate import-path candidate selection for Bevy dummy/real modules. Added manifest-owned source materialization for real projects that concatenate, generate, or specialize WGSL before shader module creation, covering wgpu timestamp normalization, wgpu copy-texture/shader-test templates, wgpu mesh shaders, wgpu ray-query vertex-return shaders, wgpu binding-array and per-vertex shaders, Naga ray-query legacy enable injection, webgpu-samples Cornell shader suffixes/template replacement, webgpu-samples skinned-mesh generated vertex input, Bevy's historical `light_probes` -> `light_probe` profile path, and primitive-index/barycentric/multiview validation capabilities. This raises Bevy to 159 source-valid / 150 composed-valid and total external corpus coverage to 479 source-valid files with 21 explicitly validated standalone-invalid negative cases. |
| `WGSL-257` | 100% naga-oil compose parity | The real-project gate proved composed outputs were Naga-valid, but it did not compare moon_wgsl against upstream `naga_oil` on the same fixture, shader defs, value defs, capabilities, and additional imports. | `DONE` | Added `testdata/external_naga_oil_compose_parity.tsv` plus `tools/check_external_naga_oil_compose_parity.sh`. The gate runs moon_wgsl and the Rust `naga_oil` oracle against critical Bevy PBR/deferred/decal/environment/SSR/SSAO profiles, then parses both outputs through Naga and compares a sorted pipeline-facing Naga IR interface fingerprint for overrides, global bindings, and entry points. Entry points must match exactly; moon_wgsl's emitted binding/override interface must be an upstream subset, because moon_wgsl intentionally performs IR reachability pruning while upstream writer output can retain unused helper globals/types/constants/functions. The parity manifest now enforces exact seven-column schema, duplicate `(repo, rel_path)` rejection, and exact case count, and it consumes the current nine-column exact-count external repo manifest instead of the old min-threshold schema. This still fails on import/name-resolution/resource-interface drift such as historical `view_bindings::clustered_lights` failures. CI now runs this gate. The investigation also exposed and fixed an unrelated IR lowering bug where `else if` conditions were dropped into unconditional `else` blocks; parser and IR regressions now cover that control-flow shape. Verified locally with the new external naga-oil compose parity gate on 2026-05-10. |
| `WGSL-256` | 100% official WGSL syntax coverage | The official CTS gate extracted static literal/object WGSL cases but did not exercise template-generated CTS sources, so parameterized type/array/matrix syntax regressions could stay outside the parser and IR contract. | `DONE` | Added `tools/extract_gpuweb_cts_template_wgsl.mjs` and wired it into `tools/check_official_wgsl_corpus.sh` beside the static validation, execution, and invalid gates. The official gate is pinned to CTS commit `3b327ebc44f11212fd3872972a6dd394634fb9e3` and now exact-gates 114 static valid cases, 111 Naga-validated static IR outputs, 3 static oracle-blocked IR outputs, 99 template-generated valid cases, 97 Naga-validated template IR outputs, 2 template oracle-blocked IR outputs, 28 execution cases, 25 Naga-validated execution IR outputs, 3 execution oracle-blocked IR outputs, 87 static invalid cases with 21 accepted by the pinned oracle, and 57 template invalid cases with 16 accepted by the pinned oracle. The extractor-generated manifests are now schema-checked and diffed against the generated `.wgsl` files, and the oracle-blocked/oracle-accepted manifests are schema-checked single-column ID lists with duplicate rejection instead of raw grep-filtered text. Template valid cases are parsed, IR-roundtripped, reparsed, and Naga-validated unless explicitly listed in `testdata/gpuweb_cts_template_ir_blocked_by_oracle.txt` for pinned-oracle unsupported `requires unrestricted_pointer_parameters`; template invalid cases are rejected unless explicitly listed in `testdata/gpuweb_cts_template_invalid_accepted_by_oracle.txt`. This exposed and fixed override-dependent array alias lowering: override declarations are now predeclared on demand before type-alias array-size expressions are lowered. Verified with the official CTS corpus gate on 2026-05-10. |
| `WGSL-255` | IR invariant validation | Naga validation checks emitted WGSL, but the internal IR can still carry invalid handles, stale type assumptions, illegal pointer/value use, or lost layout/provenance until emit or external validation exposes it indirectly. | `DONE` | Extended typed IR validation across resource binding/address-space compatibility, writable storage/lvalue store targets, access/index/swizzle legality, address-of targets, arrayLength runtime-array operands, struct/array layout invariants, atomic/image/workgroup/subgroup side-effect signatures, cast scalar widths, constructor shape checks, and Bevy immediate globals. Added focused white-box regressions for immediate globals, invalid private resource bindings, read-only storage writes, out-of-range access, invalid struct layout, fixed-array arrayLength, and address-of literals. The stricter validator exposed stale array strides created while aliases referenced predeclared struct placeholders, so lowering now refreshes dependent array layouts after type bodies are resolved instead of letting stale layout facts cross into later phases. Constructor lane checks now use expression structure where available, so swizzle/splat lane counts do not depend on unrelated scalar-type inference. Verified locally on 2026-05-09 with `moon check --target all --deny-warn`, `moon test --target all`, architecture guardrails, byte/error/preprocess parity, and external naga-oil compose parity. |
| `WGSL-254` | Real shader profile matrix | Bevy, wgpu, and sample shaders are often not standalone source files; many skipped files require concrete shader-def/value-def profiles, additional imports, source fragments, template substitutions, generated snippets, capabilities, or fuller source roots. | `IN_PROGRESS` | Added a manifest-owned external profile matrix with Bevy forward-material bool defs, sprite/UI material roots, prepass visualization defs, SSAO typed value defs, tonemapping binding-index values, deferred/environment/SSR/UI profiles, concrete meshlet culling/raster/material profiles, wgpu timestamp normalization source-prefix profiles, webgpu-samples Cornell suffix/template profiles, and a profile-owned skinned-mesh generated `VertexInput` fragment that mirrors the TypeScript shader-module construction path. The corpus gate now applies those profiles before classification, texture-atomic/texture-int64-atomic/primitive-index/barycentric/multiview/cooperative-matrix/float64/float16-in-float32/mesh-shader/ray-hit-vertex-position/per-vertex/binding-array capability detection covers corresponding real shaders, the oracle normalizes parser-unsupported vendor enable lines only inside the isolated validator, and duplicate Bevy dummy/real module candidates are resolved structurally by active requested symbols. Fixed the Bevy atmosphere `render_sky.wgsl` compose-graph request gap by making planned item imports cover execution-time cached subsets, and fixed Bevy meshlet forward aggregate layout refresh so dependent struct/array layouts are recomputed before global storage arrays are declared. The full external corpus gate now passes across Bevy, wgpu, Naga, webgpu-samples, and learn-wgpu with `repos=5 files=500 source-valid=479 composed-valid=150 ir-valid=479 expected-invalid=21 expected-failures=0 skipped=0`; the same pass exposed and fixed atomic storage texture `textureStore`, storage texture load result materialization, zero-value scalar/struct/array constructors, `modf`/`frexp` result typing, and address-of read-only storage globals. The IR roundtrip corpus and WGSL corpus matrix now keep atomics on explicit manifest-owned runtime-valid compose paths, so validation never consumes the byte-parity writer's internal Naga compare-exchange result annotation. External profile capabilities are now carried as materialized-source metadata into final emitted WGSL validation rather than being inferred again from emitted text, so profile-only validation facts cannot be lost across the IR boundary. Expected-invalid probing also exposed a real lowerer fallback: unknown address spaces such as `push_constant` were silently lowered as `private`; address-space and storage-access lowering now rejects unknown tokens, with guardrails preventing fallback reintroduction. The external gate now also probes expected-invalid files through moon IR and Naga, requiring any IR-normalized valid output to be explicitly listed in `testdata/external_wgsl_corpus_expected_invalid_normalized_by_ir.tsv`; currently the only such case is Naga's historical `math-functions.frag.wgsl` writer fixture with uncontextualized abstract-float builtin operands. Preprocessor detection in the external gate now uses comment-aware scanning and recognizes naga-oil-compatible spaced `#` directives plus `#define`, with synthetic self-tests for line-commented directives, block-comment-prefixed imports, and spaced `# define`, so future pure-define or comment-prefixed directive inputs cannot be misclassified as standalone-invalid. The external profile manifest is now checked for exact consumption, `testdata/external_wgsl_corpus_profile_modes.tsv` gates whether each profile must exercise raw materialized WGSL validation or naga-oil compose, `testdata/external_wgsl_corpus_compose_sources.tsv` exact-gates the concrete 150 Bevy source files that must exercise naga-oil compose, the repository manifest owns exact per-repo file/source-valid/composed-valid/expected-invalid counts instead of minimum thresholds, and all external corpus TSV manifests enforce exact schema width plus duplicate-key rejection. Corpus drift, malformed rows, stale rows, swapped raw/compose classifications, and accidental compose bypasses therefore cannot silently remain after upstream changes. Remaining work is to add new corpora and profile shapes rather than clearing known skips in the current gate. |
| `WGSL-253` | Differential fuzzing | The suite still relied mostly on curated regressions and harvested corpora, so broad combinations of expressions, statements, literals, types, and builtin signatures could remain untested until a real project tripped them. | `DONE` | Replaced the shell-embedded generated-source catalog with `tools/generate_wgsl_differential_case.mjs` plus a manifest-to-generator id consistency check. The generated differential gate now validates each generated source with Naga first, routes it through moon parse/IR/emit/reparse/Naga, checks required semantic tokens, and enforces category coverage for builtin, control-flow, entry, expression, layout, literal, pointer, resource, statement, and type cases. Expanded the corpus from 10 to 16 deterministic cases covering abstract composite indexing, scoped loop/switch control flow, nested lvalue writeback, numeric literal boundaries, storage texture atomics, and statement/expression call-boundary convergence. Architecture guardrails now require the deterministic generator and manifest/catalog consistency. Verified locally with the generated differential gate on 2026-05-09. |
| `WGSL-252` | Differential generated testing / pointer locals and unary consts | Generated differential probing exposed two valid WGSL patterns that moon_wgsl mishandled: pointer-valued local variables were auto-loaded when used as pointer values, so `let p = &x; inc(p);` emitted `inc(*p)`, and untyped unary abstract-int constants such as `const NEG = -1;` could not be inferred when used as switch selectors. | `DONE` | Changed local identifier lowering so local variables whose value type is `ptr` are treated as handle values instead of pointee loads, added unary expression type inference for logical-not and signed/bitwise unary operators, and added white-box plus generated differential regressions for pointer-valued locals and unary const switch selectors. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-251` | Differential generated testing / pointer store semantics | The emitter could turn direct pointer dereference compound stores such as `(*p) += 1u` into `p = ...`, assigning to the pointer parameter instead of the pointee. Naga accepts the source shader but rejects the emitted shader as an invalid assignment target. | `DONE` | Added a generated differential gate that validates generated source with Naga, lowers and emits through moon_wgsl IR, validates the emitted WGSL, and checks required semantic tokens. The new `pointer-deref-store` case exposed this bug. Fixed store emission to explicitly dereference direct pointer-valued store targets while preserving access/member auto-deref forms, added a white-box regression, and wired the differential gate into CI and architecture guardrails. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-250` | Test architecture audit / builtin and filter coverage | The test suite could pass while a whole WGSL builtin lowering class was not connected, and targeted `moon test --filter` commands could silently match zero tests. This made the suite strong for known regressions but weak as a coverage model for lowerer dispatch. | `DONE` | Added a manifest-driven WGSL builtin coverage gate that compares all lowerer-owned builtin dispatch names against explicit corpus cases and required execution stages, added static numeric/integer/atomic/barrier/ray-query builtin corpus shaders, upgraded ray query to IR plus Naga validation, added a targeted-test filter guard that fails on zero matched tests, and wired both gates into CI and architecture guardrails. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-249` | Runtime WGSL lowerer coverage / barrier builtins | Statement-level barrier builtin calls such as `workgroupBarrier()` were not classified as WGSL control/memory barrier statements. They fell through to expression lowering and could be reported as `unsupported WGSL type: workgroupBarrier`, which hid the real missing statement-lowering path. | `DONE` | Added a first-class barrier builtin classifier that lowers `workgroupBarrier` / `subgroupBarrier` into `Statement::ControlBarrier` and `storageBarrier` / `textureBarrier` into `Statement::MemoryBarrier`, rejects barrier calls in value position with a direct diagnostic, emits the corresponding WGSL builtin calls from IR, and added regressions plus architecture guardrails. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-248` | IR numeric conversion audit | Abstract integer literals contextualized to `i32` and abstract integer switch constants used `Int64::to_int()` without an explicit WGSL i32 range check, unlike the u32 path. Out-of-range abstract ints could therefore depend on MoonBit conversion behavior instead of producing a clear lowerer error. | `DONE` | Added explicit `[-2147483648, 2147483647]` checking before constructing `Literal::I32` or `SwitchValue::I32`, routed abstract signed/unsigned integer conversions through checked helpers, and added regressions for `const VALUE: i32 = 2147483648;` plus an out-of-range abstract-int switch case. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-247` | Registry replacement audit | Re-registering an existing normalized `rel_path` with a different `#define_import_path` updated the source map but left old `module_path -> rel_path` entries behind. That could make stale module names resolve to newer unrelated source text. | `DONE` | Registry insertion now removes all module-path entries pointing at the normalized rel path before installing the new module mapping. Added a regression proving the old module name no longer resolves after source replacement. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-246` | Compose graph execution audit | Compose graph execution still used empty-string sentinels for module-path lookup failures. That made missing module metadata indistinguishable from the root module path and required repeated `== ""` checks in virtual-override and final-name planning. | `DONE` | Changed `module_rel_path_for_module_path` and `module_path_for_rel_path` to return `String?`, updated graph execution to explicitly handle root vs missing-module cases, and added a guardrail preventing module-path lookup sentinels from returning. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-245` | Compose registry consistency audit | Import graph planning and semantic file-path resolution could still fabricate fallback values when a resolved registry rel path no longer had source text: import graph used an empty WGSL source, and semantic graph used the raw import path as module provenance. That can hide registry corruption or duplicate-source invalidation behind later unrelated behavior. | `DONE` | Changed import graph planning to raise `MissingRegisteredSource` when a resolved module rel path has no registered source, changed semantic file-path resolution to fail that resolution branch instead of fabricating module provenance, and added an architecture guardrail preventing registered-source lookups from falling back to empty strings or raw import paths. Verification is tracked with this audit pass on 2026-05-08. |
| `WGSL-244` | IR literal audit / f16 semantic literal model | IR represented f16 literals as `F16Bits(Int)` and the lowerer parsed `F16Literal` text through an integer parser. Legal WGSL forms such as `1.5h` or abstract-float initializers contextualized to `f16` could therefore fail despite being valid shader literals. | `DONE` | Replaced the placeholder with `Literal::F16(Double)`, lowered suffixed f16 literals through the float parser, contextualized abstract floats to f16 when an expected f16 scalar type is available, emitted f16 literals canonically with `h`, and added a regression covering `1h`, `1.5h`, and `const z: f16 = 2.0`. Added a guardrail preventing the integer-bit placeholder from returning. Verified with targeted IR testing and IR target checking on 2026-05-08. |
| `WGSL-243` | Metadata import-use audit / source fallback removal | `metadata_collect_import_uses` swallowed `InvalidWgslSyntax` from import substitution and returned the original source. That made metadata extraction silently succeed on malformed WGSL whenever import-use collection could not parse the source, hiding invalid input behind an empty/partial import-use result. Compose also used full import-use metadata just to merge local `#define` values, which let WGSL syntax failures preempt import graph diagnostics. | `DONE` | Changed metadata import-use collection to propagate invalid WGSL as `MetadataError::ImportParseError`, added a regression proving full metadata no longer succeeds on malformed WGSL, and added an architecture guardrail preventing WGSL parse failures from falling back to source text. Separated directive-only metadata collection from import-use collection via an explicit `collect_import_uses` option, and changed compose shader-def merging to use directive-only metadata so import graph planning still reports missing imports before later syntax validation. Verified with targeted metadata/upstream regressions and full target checking on 2026-05-08. |
| `WGSL-242` | Lowerer call-boundary audit / code quality review | Even after user function call arguments were converged, raw AST call argument arrays still flowed from several `Call(callee, arguments)` boundaries into specialized lowerer branches. Most branches flattened internally, but correctness still depended on every branch remembering to normalize parser legacy `Sequence` argument nodes. | `DONE` | Added `wgsl_ir_call_arguments` at every AST call-lowering boundary before semantic dispatch, covering ray-query statements, ordinary call statements, type inference, global expressions, and function expressions. Added an architecture guardrail requiring every `Call(callee, arguments)` boundary to normalize arguments before dispatch. Verified with IR tests, architecture guardrails, and the WGSL corpus matrix on 2026-05-08. |
| `WGSL-241` | Lowerer branch convergence / statement call arguments | Statement-level user function calls and expression-level user function calls lowered their argument lists through different branches. Parser-level multi-argument fixes therefore reached expression calls but could still miss ordinary call statements. | `DONE` | Added `lower_user_function_call_arguments` as the single user-function call argument path and routed both expression calls and statement calls through it. Strengthened IR tests for multi-argument expression calls and statement calls, and added an architecture guardrail preventing manual raw-argument lowering before `Statement::Call`. Verified with targeted IR tests and guardrails on 2026-05-08. |
| `WGSL-240` | GPUWeb CTS execution corpus / IR lowerer coverage | The official CTS gate only used validation fixtures, so execution shaders exposing pointer dereference values, multi-argument call parsing, lexical shadowing, `break if`, global variable type inference, and abstract-int switch selectors were not part of the lowerer quality bar. | `DONE` | Added a static GPUWeb CTS execution WGSL extractor, expanded the official corpus gate to parse/IR/reparse 28 execution shaders and Naga-validate 25 of them, with 3 pinned-Naga oracle-blocked cases still IR-gated. Fixed the uncovered parser/lowerer issues structurally: call arguments are parsed as separate AST nodes, nested blocks use child lexical scopes, local variables shadow function arguments, `break if` lowers to the IR loop condition field, explicit pointer loads and global initializer type inference are supported, and abstract-int switch selectors lower correctly. Verified with `moon test` and `tools/check_official_wgsl_corpus.sh` on 2026-05-08. |
| `WGSL-239` | Architecture follow-up / semantic path duplicate state | Compose semantic reference paths still stored both structured path segments and a normalized `text` string, creating a small but unnecessary second source of truth in alias/import reachability analysis. | `DONE` | Removed cached semantic path text, made path text derive from structured segments, and added an architecture guardrail preventing the duplicate field from returning. Verified with compose and full target checks on 2026-05-08. |
| `WGSL-238` | Architecture follow-up / IR lowerer monolith | The WGSL-to-IR lowerer had grown into a 9k+ line file mixing module orchestration, type lowering, layout computation, literal parsing, built-in name tables, attribute decoding, statement lowering, and expression lowering. This made unrelated semantic areas harder to review and easier to regress together. | `DONE` | Split type lowering, layout helpers, literal/operator conversion, built-in function/type name tables, and attribute/resource decoding into dedicated IR files while preserving package-private access. Added an architecture guardrail capping the lowerer monolith below 8000 lines. Verified with IR and full target checks on 2026-05-08. |
| `WGSL-237` | Architecture follow-up / parser generated boundary | Raw top-level item collection was still exposed as a generated parser start symbol carrying `WgslRawTopLevelItem` through `Token::ITEM`, and parser-only declaration-head/raw-top-level structures remained public because generated parser metadata needed them. This left parser staging details visible as public API. | `DONE` | Removed the generated `raw_top_level_items` start and `ITEM` token, routed strict translation-unit parsing directly through the hand scanner, removed the obsolete generated `Parse` error branch, made raw top-level and declaration-head staging types package-private, and added an architecture guardrail preventing the generated raw top-level start from returning. Verified with parser and full target checks on 2026-05-08. |
| `WGSL-236` | Architecture follow-up / strict declaration parse failures | Strict translation-unit parsing still converted known declaration parse failures into `WgslGlobalDeclData::Raw` and then raised an error only after the declaration kind became `Unknown`, so malformed structured declarations briefly passed through the AST as raw nodes. | `DONE` | Added a checked declaration-data parser path that raises on malformed known declaration heads, routed strict translation-unit construction through it directly, and kept `Raw` only for trivia/unknown top-level fragments rather than recognized declaration failures. Verified with parser tests, full target checking, and architecture guardrails on 2026-05-07. |
| `WGSL-235` | Architecture follow-up / declaration-head parser boundary | Top-level declaration classification still used a generated `decl_head` parser entry and caught failures into `WgslDeclHead::raw([])`, keeping a declaration-head-specific source fallback and an unused public generated parser surface. | `DONE` | Moved declaration-head recognition into the strict AST declaration parser as a finite token-state classifier, removed the generated `%start decl_head` grammar and its unused keyword token variants, and regenerated the private parser package metadata. Verified with parser tests, full local tests, architecture guardrails, preprocess parity, the manifest WGSL corpus matrix, and the official WGSL CTS corpus gate on 2026-05-07. |
| `WGSL-234` | Architecture follow-up / expression parse failure model | Expression parsing still carried a `WgslExprNodeKind::Token` recovery node and rejected it later in strict parsing, so malformed expression text could briefly enter the AST as a fallback node. Override type predeclaration also used an abstract-int fallback for untyped overrides whose initializer type could not be resolved in the first pass. | `DONE` | Removed the expression `Token` AST node entirely, changed parser and IR token parsing to report structural parse failure instead of manufacturing fallback nodes, and made non-explicit override type predeclaration resolve through recorded const/override declarations so forward references are typed without abstract-int fallback. Verified with parser/IR targeted checks, full local tests, and the official WGSL CTS corpus gate on 2026-05-07. |
| `WGSL-233` | Architecture follow-up / WGSL line and identifier policy | WGSL line-break and identifier validation rules remain distributed across lexer, directive scanning, top-level splitting, and type/expression parsing. CR-only, NEL, U+2028/U+2029, `_`/`__`, keyword, and reserved-word behavior can drift between subsystems. | `DONE` | Added lex-owned WGSL line-break and identifier helpers, routed tokenizer, top-level directive line slicing, metadata line slicing, preprocess blanking, and type-atom identifier checks through shared policy, and added official line terminator plus identifier regressions. Verified with full local tests on 2026-05-07. |
| `WGSL-232` | Architecture follow-up / strict expression AST | The expression parser still has a `WgslExprNodeKind::Token` fallback that can make malformed expression fragments look like parsed AST nodes. Invalid numeric fallback is blocked, but strict/tolerant expression modes are not fully separated. | `DONE` | Added structured `Assignment`, `PostfixUpdate`, and shader-def template expression nodes, made strict expression refs reject residual Token fallback nodes, and made strict function parsing fail when statement expression slots contain malformed expressions. Verified with parser, upstream mirror, and full local tests on 2026-05-07. |
| `WGSL-231` | Architecture follow-up / generated provenance | `Function.generated_import` removes emitter-side generated-name detection, but the source-only lowerer still initializes the metadata by checking whether a function name contains the naga_oil marker. Provenance is therefore not yet fully structural. | `DONE` | Added explicit generated-import lowerer entry points and replaced the remaining generated-name array plumbing with `WgslIrGeneratedImportProvenance` records carrying generated name, source rel path, original symbol name, and provenance kind. Compose now passes source-origin and virtual-override provenance as structured records, filters root-source origins, and the lowerer uses a provenance table instead of substring or name-list inference. Verified with IR, parser, compose, and full local tests on 2026-05-07. |
| `WGSL-230` | Architecture follow-up / numeric literal model | Numeric literal parsing is stricter, but AST numeric token kinds still store narrowed MoonBit values rather than a first-class WGSL literal descriptor containing raw text, base, suffix, and category. | `DONE` | Replaced narrowed `IntLiteral`/`UintLiteral`/`FloatLiteral` token variants with a single `WgslNumericLiteral` descriptor carrying raw text, base, suffix, and category. Parser classification no longer falls back to float on invalid numeric text, normalization keys use the descriptor, and IR lowering performs checked semantic conversion from raw/base/suffix/category while preserving large u32 and hex abstract-int literals. Verified with parser and full local tests on 2026-05-07. |
| `WGSL-229` | Architecture follow-up / switch integer evaluation | Switch const selector evaluation still represents integer values as `(Int, Bool)` and reinterprets `U32` through signed `Int` before converting back to `UInt`. This can reintroduce large unsigned literal bugs in switch-case const paths. | `DONE` | Replaced switch/global integer constant evaluation with typed signed/unsigned/abstract values, removed unsigned-to-signed reinterpretation from switch selectors, and added a large-u32 const switch regression. Verified with IR and full local tests on 2026-05-07. |
| `WGSL-220` | Large unsigned literal regression | Legal WGSL literals such as `3221225472u` parsed successfully and validated with Naga, but moon_wgsl IR lowering rejected them as unsupported expressions. The AST token kind stored `UintLiteral` in signed `Int`, so values above the signed i32 range could be misclassified as float literals before IR lowering. | `DONE` | Changed AST `UintLiteral` to carry `UInt`, changed parser unsigned literal parsing to accumulate unsigned values, updated IR and transform small-integer helper paths to explicitly downcast only where small integer semantics are required, and added parser/IR regressions plus `testdata/ir_corpus/large_u32_literal.wgsl` in the manifest-driven corpus matrix. Verified locally on 2026-05-07. |
| `WGSL-228` | Review finding / parity inventory gate | The naga_oil parity inventory gate proves that case labels or paths appear in shell scripts, not that the manifest rows are actually executed. Comments, dead code, or unrelated output could satisfy the check and produce false confidence. | `DONE` | Added `--list` modes to the byte/error parity gates, aligned their case labels with `parity_manifest.tsv`, and changed the inventory gate to compare manifest byte/error rows against machine-readable executed label lists instead of grepping shell text. Verified with the parity inventory gate on 2026-05-07. |
| `WGSL-227` | Review finding / parser numeric strictness | The expression parser still narrows WGSL integer tokens into MoonBit `Int`/`UInt` too early and can fall back to `FloatLiteral` when numeric classification fails. Strict parsing can therefore accept invalid WGSL or lose AbstractInt contextual conversion semantics. | `DONE` | Added `InvalidNumericLiteral` token classification and made expression-ref parsing fail instead of treating invalid numeric text as a float. Tightened float recognition for malformed suffix/exponent tokens while keeping legal `0.` style float literals. Added strict parser regressions for `1foo`, `0x`, and `1e+`. Verified with targeted parser/IR/compose tests and the WGSL corpus matrix on 2026-05-07. |
| `WGSL-226` | Review finding / generated provenance | IR emission still detects generated import functions by checking whether the final function name contains `X_naga_oil_`. That reintroduces generated-name provenance inference from strings. | `DONE` | Added typed `Function.generated_import` metadata and changed the emitter to use that field instead of inspecting the final function name. The default source-only lowerer initializes the metadata while parsing generated compose output, keeping provenance decisions outside the emitter. Verified with targeted IR tests on 2026-05-07. |
| `WGSL-225` | Review finding / reference rewrite identity | Reference rewrite still selects targets primarily from visible text names, with symbol identity used mostly to avoid rewriting ambiguous names. Distinct imported identities sharing a name can therefore leave unresolved references or silently depend on fallback behavior. | `DONE` | Tightened compose before transform by rejecting same-scope alias bindings that expose one visible reference path for multiple symbol identities, so transform no longer receives the known bad same-alias ambiguous plan. Updated the old issue 5 same-alias collision regression to require an ambiguity diagnostic instead of silent success. Verified with the full local test matrix on 2026-05-07. |
| `WGSL-224` | Review finding / alias binding conflicts | Alias/import binding insertion can silently discard a later binding when `scope + alias_name + from_name` matches an existing entry but the symbol identity differs. This makes behavior depend on import order. | `DONE` | Extended `WgslComposeBindingSet` with conflict tracking and changed compose finalization to raise `AmbiguousImportBinding` before rewrite when alias/import bindings collide by visible key but differ by identity. Verified with targeted compose checks on 2026-05-07. |
| `WGSL-223` | Review finding / IR control-flow semantics | IR lowering can change WGSL `while` into do-while behavior by checking the condition after the first loop body execution. | `DONE` | Changed `while` lowering to emit a top-of-loop condition guard that breaks before executing user statements when the condition is false. Added IR emitter regression coverage proving the emitted loop has no `continuing` condition and checks before body statements. Verified with targeted IR tests on 2026-05-07. |
| `WGSL-222` | Review finding / IR struct layout preservation | IR roundtrip can drop struct member layout decorations such as `@size` and `@align`, so Naga validation may pass while uniform/storage ABI changes. | `DONE` | Added explicit `size` and `align` metadata to IR `StructMember`, recorded explicit WGSL layout attributes during lowering, emitted them back to WGSL, and added roundtrip coverage for `@size(16) @align(16)` struct members. Verified with targeted IR tests on 2026-05-07. |
| `WGSL-221` | Review finding / typed shader-def values | Compose/preprocess still route `ShaderDefValue::UInt` through signed `Int` value defines using `reinterpret_as_int`, so large unsigned shader-def values can become negative or compare incorrectly. | `DONE` | Changed compose/template-const `value_defines` from `HashMap[String, Int]` to `HashMap[String, ShaderDefValue]`, kept only numeric shader-def values in the template-const replacement channel, and removed unsigned-to-signed reinterpretation. Added a compose regression for `3221225472U` template replacement. Verified with targeted compose tests on 2026-05-07. |
| `WGSL-219` | Coverage architecture review | WGSL coverage was split across multiple hardcoded shell scripts: static IR fixtures, upstream compose fixtures, generated real-shader validation cases, and downstream regressions each had separate execution paths. That made it too easy to expand one gate while leaving another stage untested, and it did not provide one manifest that states which pipeline stages each real WGSL case must pass. | `DONE` | Added `testdata/wgsl_corpus_manifest.tsv` and `tools/check_wgsl_corpus_matrix.sh`. The manifest classifies file, compose, and generated cases, then runs declared stages (`compose`, `parse`, `naga`, `ir`, `naga-ir`) with optional shader capabilities. The initial matrix covers the local IR corpus, upstream compose mirrors, diagnostic/issue regression generators, full Bevy PBR forward, MGStudio mesh3d forward, ray query, and dual-source blending. CI and architecture guardrails now require the manifest-driven matrix. Verified locally on 2026-05-07. |
| `WGSL-218` | Naga_oil parity gate review | The upstream compose expected fixture inventory was still partly encoded as shell-script control flow, and moon_wgsl source-level error parity did not have its own gate. That made byte parity, semantic parity, oracle diagnostics, and source-level error parity harder to audit as separate classes. | `DONE` | Added `testdata/naga_oil_upstream/compose_tests/parity_manifest.tsv`, which classifies every mirrored upstream expected fixture as byte, semantic, or error. Added `tools/check_naga_oil_parity_inventory.sh` to require complete manifest coverage and ensure byte/error rows are wired into their concrete gates. Added `tools/check_moon_wgsl_error_parity.sh` plus `tools/compose_case --expect-error` to gate source-level missing-import, invalid-override, parser, and validator diagnostics. CI and architecture guardrails now require the inventory and error-parity gates. Verified locally on 2026-05-09. |
| `WGSL-217` | Validation architecture review | The validation gate still depended on Naga as the deepest executable oracle. Naga validates WGSL syntax and many IR semantics, but it does not fully exercise wgpu pipeline layout compatibility, so runtime binding-access mismatches such as read-only storage layouts versus read-write shader globals could regress without a native WebGPU gate. Directly adding `wgpu_mbt` to the root module would contaminate the published library with a native-only dependency. | `DONE` | Added an isolated `tools/wgpu_validation` MoonBit subproject with its own `moon.mod.json` and native-only `Milky2018/wgpu_mbt` dependency. Added `tools/check_wgpu_validation.sh`, which builds the subproject binary, validates shader-module creation for generated Bevy PBR and MGStudio forward shaders, IR-roundtrips a storage-access fixture, validates it against an explicit read-only/writable storage pipeline layout, and includes a negative control that proves wgpu rejects read-write shader access against a read-only layout. CI now updates the isolated subproject and runs the wgpu gate without adding `wgpu_mbt` to the root module dependency graph. Verified locally on 2026-05-07. |
| `WGSL-216` | Architecture risk review | The transform reference rewrite plan still exposed symbol bindings as `(rel_path, original_name, from_name, to_name)` arguments at the public boundary, so callers could construct an identity-shaped binding without actually carrying the canonical IR identity object. The validation improvements from `WGSL-215` also had no single guardrail that would fail if future changes reintroduced CTS allowlists, cached alias phases, unscoped rewrite APIs, or dropped real Bevy/MGStudio IR validation. | `DONE` | Changed `WgslReferenceRewritePlan::add_symbol_binding` to require `WgslIrSymbolIdentity` directly and changed transform rename rules to keep optional identities instead of separate provenance strings. Added `tools/check_architecture_guardrails.sh`, which rejects the old official CTS allowlist path, cached qualified alias binding phases, public unscoped string rewrite APIs, reference rewrite bindings with split provenance strings, and missing full Bevy PBR / MGStudio IR validation gates. Unsupported expression lowering now reports explicit AST node categories instead of a catch-all fallback. Verified locally on 2026-05-07. |
| `WGSL-215` | Architecture risk review | The validation surface still allowed three structural blind spots: the official CTS IR gate depended on a handwritten allowlist, real Bevy/MGStudio forward shaders were validated only as composed source rather than as `compose -> IR -> emit` output, and composer still exposed a public unscoped string reference-rewrite API plus a cached alias binding phase distinct from ordinary identity bindings. | `DONE` | Removed `testdata/gpuweb_cts_ir_allowlist.txt`; the official corpus gate now requires every extracted static-valid case to parse, lower to IR, emit WGSL, and parse again, with only Naga validation skipped for oracle-blocked subgroup-enable cases. Added Bevy PBR functions, full Bevy PBR forward, and MGStudio mesh3d forward compose outputs to the WGSL validation gate, each with IR roundtrip plus Naga validation. Removed public `WgslReferenceRewritePlan::add`, deleted `CachedQualifiedAliasBinding`, and made alias binding dedup preserve same-identity final-name correction while rejecting cross-identity alias collisions. Verified locally on 2026-05-07. |
| `WGSL-214` | Parser / IR structural coverage | Real Bevy PBR IR roundtrip exposed two core WGSL lowering gaps: the statement parser could attach `continuing` following an `if` block to the `if` statement instead of keeping it as the loop continuing clause, and IR lvalue lowering did not treat `ptr<function, T>` function arguments as addressable values for WGSL's automatic pointer-member access/writeback. | `DONE` | Restricted statement continuation so `else` only continues `if` statements and `continuing` only continues `loop` statements. Changed loop lowering to split a `continuing` clause out of the loop body's statement list into the IR loop continuing block. Added pointer function-argument lvalue lowering and parser regressions for nested `if` conditions plus loop continuing ownership. Verified with full Bevy PBR/MGStudio compose-to-IR validation on 2026-05-07. |
| `WGSL-213` | Official WGSL CTS gate coverage | Three official static-valid subgroup enable cases were documented as blocked by the pinned Naga oracle, but the gate only covered them through parser mode. That left a small blind spot where moon_wgsl could regress IR lowering/emission for those directive-only modules without the official corpus gate noticing. | `DONE` | Changed `tools/check_official_wgsl_corpus.sh` so oracle-blocked cases must still lower to IR, emit WGSL, and be parsed again by moon_wgsl. They remain excluded only from Naga validation because the current pinned Naga WGSL frontend rejects `enable subgroups;` before validation capabilities apply. Verified locally on 2026-05-07 with the official GPUWeb CTS corpus gate. |
| `WGSL-212` | Official WGSL CTS / IR semantic preservation | The AST/parser recognized global and function `const_assert`, but IR lowering silently discarded it. That hid real semantic failures in the official CTS: once assertions are preserved, abstract-float constants must remain abstract inside const/assert expressions, and global constant expressions also need member/index access such as `x.x`. | `DONE` | Added top-level `Module.const_asserts`, `Statement::ConstAssert`, IR lower/emit/validation support, global expression member/index lowering, and const/assert-specific abstract-float emission so ordinary naga_oil writer parity still emits concrete float suffixes where expected. Added unit coverage plus an IR corpus fixture that validates `const_assert` precision through the naga oracle. Verified locally on 2026-05-07 with `moon test`, all parity gates, IR corpus, WGSL validation, and official GPUWeb CTS corpus. |
| `WGSL-211` | Parity gate hardening | The pinned `naga_oil` oracle's `override_top` WGSL writer output can nondeterministically swap the virtual-base and override helper function order between processes because that order comes from upstream/Naga internals rather than a stable source-order contract. The local byte-parity gate stayed deterministic, but the upstream-oracle parity gate could fail before testing moon_wgsl changes. | `DONE` | Changed `tools/check_preprocess_parity.sh` to keep exact comparison when the oracle emits the pinned byte order, while accepting the equivalent alternate helper order only after asserting the virtual helper, module helper, dispatch call, and top call are all present. This keeps the gate focused on virtual override semantics instead of upstream writer ordering noise. Verified locally on 2026-05-07. |
| `WGSL-210` | Runtime wgpu validation / Bevy follow-up | Real shader execution advanced past import/name resolution but failed later in validation because IR lowering changed default `var<storage>` access from WGSL's read-only default into read-write, producing shader `Storage { access: LOAD | STORE }` for bind-group layouts that expect read-only storage. The same path also left `arrayLength` disconnected from the IR expression lowerer even though the IR data model had an `ArrayLength` node, so calls fell into constructor type inference and failed as `unsupported WGSL type: arrayLength`. | `DONE` | Changed storage address-space lowering so omitted storage access lowers to `StorageAccess::load()` for both `var<storage>` and `ptr<storage, T>`; explicit `read_write` still lowers to load/store. Connected `arrayLength(&runtime_array)` to `Expression::ArrayLength`, added emit/type inference support, and added validation-gate coverage that roundtrips storage runtime arrays through IR and validates the emitted WGSL with the naga/wgpu oracle. Verified locally on 2026-05-07. |
| `WGSL-209` | W3C WGSL Editor's Draft 2026-05-05 / syntax audit | Strict WGSL parsing did not accept official top-level empty declarations (`;`), and the statement AST modeled `case` selectors only as expressions. That made `case 0u, default, :` parse superficially but lower `default` as an identifier. Control-flow condition extraction also used the closing parenthesis as a boundary too aggressively, which could either include switch-body attributes in selectors or drop trailing condition operators after grouped operands. | `DONE` | Added `WgslCaseSelector` with explicit `DefaultSelector` / `ExpressionSelector`, changed `WgslStatementPayload::Case` to carry structured selectors, and lowered mixed `case` selectors into IR `SwitchValue::Default` or integer expression cases. Top-level semicolons are now parsed as trivia/empty declarations. Control-flow condition slicing now ends at the official compound-statement/statement-attribute boundary while preserving grouped operand tails such as `(flags & FLAG) != 0u`. Added parser and IR regressions for top-level empty declarations, switch body attributes, mixed default selectors, and IR emission/validation. Verified with targeted parser/IR tests on 2026-05-07. |
| `WGSL-203` | Architecture review / Naga IR parity | The project had AST and compose-specific semantic binding IR, but no full shader IR equivalent to Naga's `naga::ir::Module`, leaving no canonical representation for future validation, lowering, or backend-style analysis. | `DONE` | Added a new `ir` package modeled after Naga's shader IR data model: module arenas/handles, type system, constants, overrides, globals, expressions, statements, functions, entry points, diagnostic filters, mesh/ray/subgroup/cooperative constructs, and bitflag-like wrappers. The package intentionally disables `unused_constructor` because a complete IR taxonomy must expose constructors before all lowerers consume every variant. Added IR construction tests and generated the public interface. Verified with full local gate on 2026-05-06. |
| `WGSL-202` | GitHub issue #13 | Bevy PBR composition could rewrite `bevy_pbr::shadows` internal `view_bindings::view` global references to an alias-scoped generated name such as `shadows__view_bindings__view` even when that generated global declaration was not emitted. | `DONE` | Root cause was cached alias binding replay plus same-identity final-name conflict: live binding selection could keep a cached alias rewrite solely because the source referenced that alias path, and transform then saw multiple final names for the same symbol identity. Live binding planning now resolves undeclared binding targets through the actually emitted declaration name for the same stable symbol identity, while preserving declared alias-scope targets when they exist. Added issue 13 compose and WGSL validation regressions. Verified with full local gate on 2026-05-06. |
| `WGSL-201` | W3C WGSL Candidate Recommendation Draft 2026-03-10 | The AST/parser still treated type template arguments as type refs only and did not preserve official statement/function-body attributes, leaving gaps against the standard grammar's `template_arg_expression` and attributed `compound_statement` forms. | `DONE` | Added ordered type template arguments that can carry either nested type refs or expression refs, so constructs like `array<vec4<f32>, N + 1u>` preserve the size expression and its identifiers. Statement AST nodes now preserve leading attributes and classify `continuing`, `case`, and `default` clauses directly; function AST nodes preserve attributes attached to the function body compound statement. Added official-grammar regressions for `enable`, `requires`, `diagnostic`, workgroup size, statement diagnostic attributes, loop continuing blocks, switch clauses, and expression template arguments. Verified locally with parser tests and full gate on 2026-05-06. |
| `WGSL-200` | Composer architecture review | Final generated names were still assigned while source emission was running, and the previous issue 12 fix used canonical final-name state for alias-scoped declarations, which could collapse valid naga_oil duplicate alias emissions by route order. | `DONE` | Added a separate graph-planning final-name assignment table that is populated before emission and consumed by emission. The canonical symbol-identity representative is first-writer-wins during graph planning, while alias-prefixed assignments and cached full-module visits are separately scoped by the complete alias-prefix path. Parent aliases only expose declarations owned by that module instead of transitive dependency origins, and direct item imports always install a local unqualified symbol binding whether they reuse a visible identity or emit the declaration themselves. Emit now receives alias-prefix scope explicitly, so declaration rename decisions use the planned table rather than ad hoc alias rewriting. Verified with full local gate on 2026-05-06. |
| `WGSL-199` | GitHub issue #12 | `0.9.3` lost Bevy tonemapping `ColorGrading` type identity when the same `bevy_render::view::ColorGrading` declaration was emitted through an alias-flattened `mesh_view_bindings` path but referenced through a transitive unqualified item import in `tone_mapping`. | `DONE` | Alias compose bindings for nested imports now use `source_origins` to carry the real declaration identity instead of attributing every alias binding to the immediate imported module, and unqualified item imports can reuse a currently visible binding for the same stable identity. Added an issue 12 regression covering the Bevy PBR tonemapping source shape. Verified with full local gate on 2026-05-06. |
| `WGSL-198` | Composer architecture review | Transform accepted an identity-backed `WgslReferenceRewritePlan` but immediately collapsed it into a plain `HashMap[String, String]`, so two symbol identities sharing the same visible reference name could be overwritten before AST rewrite. | `DONE` | Removed the binding-plan `redirect_map` / `symbol_redirects` downgrade path. Binding plans now build `WgslRenamePlan` rules directly, rename maps keep rule lists instead of lossy string maps, and ambiguous same-reference/different-target bindings are left unresolved instead of being overwritten. Virtual override and override-target rewriting now consume binding plans directly. Added white-box coverage for ambiguous identity bindings. Verified with full local gate on 2026-05-06. |
| `WGSL-197` | Composer architecture review | Compose inferred nested import provenance by splitting generated declaration names on alias marker substrings, so final-name recording still depended on generated-name shape instead of stable source-origin facts. | `DONE` | Removed `record_wgsl_nested_flattened_item_names`; nested and flattened imported declarations now reach final-name tables through `source_origins` only. Full local tests stayed green after deleting the generated-name inference path, confirming existing provenance is carried structurally. Verified with full local gate on 2026-05-06. |
| `WGSL-196` | Composer architecture roadmap | Composer final rewrite still consumed accumulated alias redirects instead of a single final-name table keyed by stable symbol identity, so declarations and references could diverge when new import shapes were added. | `DONE` | Introduced explicit IR-owned `WgslIrSymbolIdentity` / `WgslIrAliasSymbolIdentity`, centralized canonical plus alias-scoped names in `WgslIrFinalNameTable`, added scoped final-name queries, routed final-name writes through session recorders that update IR link-graph reachability, and changed finalization to build identity-backed `WgslComposeBinding` / `WgslLiveBindingPlan` values before passing an opaque `WgslReferenceRewritePlan` to transform. Verified with full local gate on 2026-05-06. |
| `WGSL-195` | Composer architecture roadmap | Reachability was interleaved with recursive source emission, import request execution, and side-effect checks, so the composer could not reason about all reachable symbols before assigning names. | `DONE` | Added a session-level graph execution pass: root composition now calls `complete_wgsl_compose_graph_execution` before source emission, recursively plans implicit and explicit import requests, records all import edges and requested-item reachability into the IR-owned `WgslIrLinkGraph`, and only then enters the existing emit/final-name pipeline. Emit consumes planned request keys instead of recording graph edges itself. Added white-box coverage proving graph reachability is complete while final-name tables are still empty before emission. Verified with full local gate on 2026-05-06. |
| `WGSL-194` | Composer architecture roadmap | Import/dependency graph construction is still implicit in recursive compose calls instead of represented as explicit graph nodes and edges. | `DONE` | Added explicit link graph construction, now owned by IR as `WgslIrLinkGraph`, with declaration symbol nodes and import edges preserving importer rel-path, target rel-path, module path, alias, item name, and edge kind. Link graph symbol nodes carry stable IR symbol identity only; final names live in IR final-name tables. The graph execution pass records import edges and requested-item reachability before source emission begins. Verified with compose checks and Bevy parity tests on 2026-05-06, then migrated from compose-private graph storage to IR graph storage. |
| `WGSL-193` | Composer architecture roadmap | The composer still had alias redirect state (`alias_redirects`, `composed_alias_redirects`, cached redirect collection) that acted as a second binding model beside the new final-name tables. | `DONE` | Deleted the residual alias-redirect adapter from composer internals: `WgslScopedAliasRedirectSet` was replaced by identity-backed `WgslComposeBindingSet`, bindings store `from_name` / `to_name` directly instead of wrapping `WgslSymbolRedirect`, cached import replay produces compose bindings, and finalization builds a `WgslLiveBindingPlan` before calling transform. User-supplied `WgslComposeOptions.redirects` remains as the explicit public redirect feature, separate from import binding. Verified with full local gate on 2026-05-06. |
| `WGSL-192` | Composer architecture roadmap | Composer still emits/reparses source fragments across phases instead of parsing each active module once and reusing AST-backed declaration/reference facts. | `DONE` | Added a compose-session semantic fact cache keyed by active source, carrying AST-backed emitted declarations and semantic reference-path sets. Import liveness, final declaration recording, and live alias redirect filtering now reuse these facts instead of independently reparsing the same source. Verified with `moon check compose --deny-warn` and Bevy parity tests on 2026-05-06. |
| `WGSL-191` | GitHub issue #11 | `0.9.2` could emit a Bevy PBR shader that referenced `types__POINT_LIGHT_FLAGS_SPOT_LIGHT_Y_NEGATIVE` without a matching definition because alias-scoped import emission was cached as the module's default imported item name and later reused by an unrelated unqualified item import. | `DONE` | Replaced the cross-stage `imported_item_names` string cache with symbol-identity final-name tables keyed by `(module rel_path, original declaration name)`, plus a separate alias-scoped table keyed by `(alias, symbol identity)`. Aliased imports no longer populate canonical unqualified bindings, and issue 11 now has a real Bevy PBR regression asserting the point-light flag use and definition share the generated name. Verified with targeted Bevy parity tests on 2026-05-06. |
| `WGSL-190` | GitHub issue #10 | `0.9.1` could spin at high CPU while composing real Bevy PBR shaders because live alias redirect collection reparsed the entire composed source once per cached qualified alias redirect, and repeated cached imports let session alias redirects grow with duplicates. | `DONE` | Built a one-parse semantic reference-path set for live alias redirect checks, changed cached qualified alias checks to query the set, deduplicated scoped alias redirects by `(scope, from_name, to_name)` before both local use and session accumulation, and added issue 10 regressions for cached alias redirect stress plus Bevy PBR forward fragment completion. Verified with targeted Bevy parity tests on 2026-05-06. |
| `WGSL-189` | Release validation | `moon publish` packaged the old script-driven generated parser path incorrectly for downstream users, so published archives could lose moonyacc parser symbols or depend on repository-local helper scripts. | `DONE` | Replaced the private generator wrapper with package-local `pre-build` commands that call `$mod_dir/.mooncakes/moonbitlang/yacc/moonyacc $input --input-mode array -o $output && moonfmt -w $output` directly for both parser grammars. The generated parser outputs are committed as normal source and can also be regenerated by downstream package checks. Verified with extracted-package publish validation on 2026-05-06; the only dry-run failure was the already-published `0.9.0` version conflict. |
| `WGSL-188` | Architecture review | `parser` still publicly owned `#import` payload parsing and `ParseImportsError`, so preprocessor/import syntax errors were attached to the WGSL translation-unit parser boundary. | `DONE` | Split import target parsing into `import_syntax` with its own moonyacc grammar, rewired compose/resolver/transform to depend on it, and removed import payload parsing from parser. Verified with the full local gate on 2026-05-06. |
| `WGSL-187` | Architecture review | After moving one-line directive parsing out of `parser`, `parser` still depended on the broad `directive` package, pulling source scanning and shader-def semantics into the parser dependency surface. | `DONE` | Split low-level directive line / GLSL version parsing into `directive_syntax`, rewired parser and directive to depend on that narrow package, and kept source scanning plus shader-def/reference semantics in `directive`. Verified with the full local gate on 2026-05-06. |
| `WGSL-186` | Architecture review | `directive` still depended on `parser` solely for one-line directive parsing, which forced `parser` to keep a public preprocessing directive entry point and retained a generated yacc start symbol for non-WGSL source-scanner work. | `DONE` | Moved one-line directive parsing into `directive`, removed the parser-to-directive generated start rule and tokens, rewired parser internals to consume the directive package, and dropped the reverse `directive -> parser` dependency. Verified with the full local gate on 2026-05-06. |
| `WGSL-185` | Architecture review | AST still exposed zero-production-use type spelling/span convenience methods (`WgslTypeAtom::text`, `WgslTypeRef::start`), leaving raw type text and unused span access on the public surface after type atoms became structured. | `DONE` | Demoted `WgslTypeAtom::text` to package-private, removed unused public `WgslTypeRef::start`, regenerated interfaces, and verified `moon ide analyze` no longer reports public functions with zero usage. Verified with the full local gate on 2026-05-06. |
| `WGSL-184` | Architecture review | Parser still publicly owned directive source scanning and GLSL version preprocessing helpers, so downstream packages could keep treating parser as the preprocessor scanner boundary instead of a syntax boundary. | `DONE` | Moved directive source scanning and GLSL version parsing into `directive`, moved raw import brace balancing into `lex`, exposed only the single-line directive syntax parser from `parser`, and rewired compose/metadata/preprocess to consume the directive package scanner. Verified with the full local gate on 2026-05-06. |
| `WGSL-183` | Architecture review | Parser still publicly owned preprocessing directive semantic helpers and shader-def/template reference scanners, so downstream packages consumed parser as a preprocessor utility surface instead of a syntax boundary. | `DONE` | Added a dedicated `directive` package for preprocessor directive semantics, moved effective directive interpretation plus shader-def comparison/reference/template scanning there, rewired metadata/preprocess to use it, and kept parser focused on directive scanning plus WGSL parsing. Verified with the full local gate on 2026-05-06. |
| `WGSL-182` | Architecture review | `metadata` and `preprocess` depended on `analysis` solely to use import-substitution state, mixing source-transform ownership with semantic-analysis naming and making the dependency graph misleading. | `DONE` | Renamed the `analysis` package to `transform`, moved all AST-driven source transformation and canonical token emission APIs under that boundary, and rewired compose/export/metadata/preprocess to depend on `transform`. Verified with the full local gate on 2026-05-06. |
| `WGSL-181` | Architecture review | Parser still exposed metadata-only shader-def convenience APIs (`wgsl_shader_def_comparison_def`) and an unused operator-to-text method, keeping preprocessing interpretation details in the parser public surface. | `DONE` | Removed the unused operator text method, moved shader-def comparison name extraction into metadata, and left parser responsible only for returning the structured comparison node. Verified with the full local gate on 2026-05-06. |
| `WGSL-180` | Architecture review | `parser.parse_wgsl_import_alias_prefix` exposed compose-only alias-renaming policy as if it were import grammar, widening parser public API beyond syntax parsing. | `DONE` | Moved alias-prefix sanitization into `compose` as an internal semantic-import policy, removed the parser export, and moved the white-box tests to compose. Verified with the full local gate on 2026-05-06. |
| `WGSL-179` | Architecture review | `analysis.strip_wgsl_import_declarations_strict` exposed a source-to-source import-stripping helper even though the only production consumer needed source-catalog declaration metadata. | `DONE` | Made import stripping private to `analysis`, added `collect_wgsl_source_catalog_declarations_strict`, and changed compose source-catalog construction to consume declarations directly instead of a stripped source string. Verified with the full local gate on 2026-05-06. |
| `WGSL-178` | Architecture review | `analysis.rewrite_wgsl_symbol_redirects` remained as a public source-to-source rewrite facade even though production composition only needs redirect-map construction plus scoped semantic rewrite paths. | `DONE` | Removed the public facade, changed compose preparation to build the redirect map and call the existing reference-rename path directly, moved symbol-redirect rewrite coverage into analysis white-box tests, and dropped the root test-only dependency on `analysis`. Verified with the full local gate on 2026-05-06. |
| `WGSL-177` | Architecture review | `lex` still publicly exposed a generic canonical token replacement writer (`WgslTokenReplacement`, token-stream/source emitters, and absolute-fragment tokenization). That left a reusable source-level writeback primitive outside the semantic analysis owner. | `DONE` | Moved the canonical token emitter and import-substitution implementation into `analysis`, made token replacement and emitter functions package-private, and changed metadata/preprocess to consume only `analysis.WgslImportSubstitutionState`. Verified with the full local gate on 2026-05-06. |
| `WGSL-176` | Architecture review | Newly structured directive tail/source helper getters exposed internal parser-only representation details that had no cross-package users, keeping unnecessary public surface after the source-payload split. | `DONE` | Demoted unused `WgslDirectiveTail` internals and scan-source start access to package-private helpers, and removed the unused public `WgslDirective.tail_node()` escape hatch. Verified with the full local gate on 2026-05-06. |
| `WGSL-175` | Architecture review | `WgslDirectiveScanItem` still exposed raw `original_trivia()` and `cleaned_directive_text()` methods directly on semantic scan items, encouraging downstream code to treat directive items as unstructured text records. | `DONE` | Added `WgslDirectiveScanSource`, moved original/cleaned source text into that value object, and changed preprocess, metadata, and compose callers to cross the explicit scan-source boundary. Verified with the full local gate on 2026-05-06. |
| `WGSL-174` | Architecture review | Directive scanner stored `required_tail_space` as a separate boolean even though separator validity is part of the parsed directive tail syntax. This left conditional/directive dispatch with another scanner-level source of truth. | `DONE` | Moved required-separator state into `WgslDirectiveTail`, changed preprocess and metadata directive dispatch to read `WgslDirective.has_required_tail_separator()`, and removed the scanner boolean plus helper parser heuristic. Verified with the full local gate on 2026-05-06. |
| `WGSL-173` | Architecture review | `WgslDirective` stored tail text, tail start, and tail end as parallel scalar fields, so directive payload spelling and source span could drift independently. | `DONE` | Added `WgslDirectiveTail`, changed `WgslDirective` to store the tail as one value object, and updated moonyacc directive construction to build that structured tail. Verified with the full local gate on 2026-05-06. |
| `WGSL-172` | Architecture review | `WgslDirectiveKind::Other(String)` duplicated the unknown directive name already stored as `WgslDirective.name`, leaving two sources of truth for custom directive spelling. | `DONE` | Removed the `Other` payload and changed effective directive handling to read custom directive names from `WgslDirective.name()`. Verified with the full local gate on 2026-05-06. |
| `WGSL-171` | Architecture review | Shader-def comparison operators were stored as raw strings, so preprocessing condition evaluation still depended on text matching for a finite operator set. | `DONE` | Added `WgslShaderDefOperator`, changed `WgslShaderDefComparison` to store the enum, and moved unknown-operator text conversion to the diagnostic boundary. Verified with the full local gate on 2026-05-06. |
| `WGSL-170` | Architecture review | Expression tokens and expression AST nodes still represented operators as raw strings, leaving precedence, normalization, and unary/binary node semantics dependent on text literals. | `DONE` | Added `WgslExprOperator`, changed operator tokens plus unary/binary expression nodes to store structured operators, and routed precedence/normalization through the enum API. Verified with the full local gate on 2026-05-06. |
| `WGSL-169` | Architecture review | `WgslTypeRef` stored every type atom as an unclassified string, so numeric template arguments and malformed atoms could flow through dependency collection and rename planning like real type identifiers. | `DONE` | Added `WgslTypeAtom` with identifier/numeric/unknown categories, changed `WgslTypeRef` to store that atom, and made dependency collection plus type-reference rewrite operate only on identifier atoms. Verified with the full local gate on 2026-05-06. |
| `WGSL-168` | Architecture review | Raw top-level items and global declarations still carried declaration-name strings derived from declaration text, creating a second source of truth beside `WgslGlobalDeclData`. | `DONE` | Removed `WgslRawTopLevelItem.name`, removed the `WgslGlobalDecl` cached name field and constructor argument, and made graph/declaration names derive from structured declaration data. Verified with the full local gate on 2026-05-06. |
| `WGSL-001` | Local audit | Composer/export path does not report import cycles explicitly; recursive item imports can fail silently or truncate output. | `DONE` | Added `ComposerError::ImportCycle` and active import-stack checks in recursive composition. Verified with `moon test` on 2026-04-15 (`28/28` passing), including a new cycle regression test. |
| `WGSL-002` | Local audit | Registry APIs do not diagnose duplicate module names or conflicting rel-path ownership when bulk-registering source files. | `DONE` | Added `analyze_source_files` and `register_source_files_checked`. Verified with `moon test` on 2026-04-15 (`30/30` passing), including duplicate module-name and normalized rel-path conflict cases. |
| `WGSL-003` | Local audit | Source-map output is declaration-level and best-effort; ambiguous same-name declarations across registered files only surface as warnings. | `DONE` | Added `build_registered_source_catalog` and exposed `source_catalog` on `WgslExportOutput`. Verified with `moon test` on 2026-04-15 (`31/31` passing), including an ambiguous declaration regression test. |
| `WGSL-004` | Portable I/O support | Cross-backend WGSL source tree scanning is not implemented yet, even though `moonbitlang/x/fs` now provides portable file I/O primitives. | `DONE` | Added `WgslSourceScanOptions`, `scan_wgsl_source_files`, `scan_wgsl_source_files_checked`, plus `Composer::register_source_tree` and `Composer::register_source_tree_checked` on top of `moonbitlang/x/fs`. Verified with `moon test` on 2026-04-15 (`41/41` passing), including source-tree scanning, duplicate-module diagnostics, and source-tree-backed composition regressions. |
| `WGSL-005` | Upstream parity gap | Source-level `override` / redirect support is still missing from the MoonBit port. | `DONE` | Added `WgslSymbolRedirect`, `rewrite_wgsl_symbol_redirects`, and redirect-aware composer/export APIs. Verified with `moon test` on 2026-04-15 (`34/34` passing), including redirect-aware composition and tree-shaking regression tests. |
| `WGSL-006` | Architecture review | `Composer::add_composable_module` maintains local module metadata, but compose/export still resolve through a separate registry path, so `Composer` is not the actual source-of-truth for composition. | `DONE` | Added Composer-owned registry/module maps plus Composer-scoped registry APIs, and wired `add_composable_module` into that state. Verified with `moon test` on 2026-04-15 (`37/37` passing), including Composer-local registry and composable-module integration tests. |
| `WGSL-007` | Architecture review | Export `source_catalog` / `source_map` currently derive from all registered files instead of the dependency closure of the current compose session. | `DONE` | Export now builds source catalogs and source maps from `PreparedWgslSource.source_files` instead of the full registry. Re-verified with `moon test` on 2026-04-30 (`192/192` passing), including dependency-closure scoping regression coverage. |
| `WGSL-008` | Architecture review | Public compose APIs leak internal session state (`visited`, `modules`) instead of exposing a stable config/session abstraction. | `DONE` | Removed public `WgslComposeSession` exposure and replaced cross-package export coupling with `Composer::prepare_wgsl_source` returning `PreparedWgslSource`. Re-verified with `moon test` on 2026-04-30 (`192/192` passing). |
| `WGSL-009` | Upstream parity gap | The MoonBit port still lacks a broad upstream `compose` parity corpus, so regressions in aliasing, conditional imports, declaration usage, and namespace-style imports are under-tested. | `DONE` | Added a local `testdata/upstream_compose` fixture corpus plus 20 upstream-inspired parity tests covering simple compose, shader defs, item imports, conditional imports, missing imports, declaration dependencies, alias collisions, diagnostic directives, dual-source blending, and duplicate-import scopes. Verified with `moon test` on 2026-04-15 after landing the new cases. |
| `WGSL-010` | Upstream parity gap | Module-alias imports currently flatten `alias::name` into plain `name` without import-scoped renaming, which risks declaration collisions and wrong self-recursion when imported items share local names. | `DONE` | Added alias-scoped source-level renaming plus importer-scoped item-import visit keys, so upstream-style `dup_import`, `dup_struct_import`, and `call_entrypoint` scenarios now compose/export successfully without Naga mangling. Verified with `moon test` on 2026-04-15. |
| `WGSL-011` | Upstream parity gap | Namespace-style qualified module references such as the upstream `rusty_imports` case still rely on paths like `a::x::square(...)` without an explicit `#import`, which the current source-level resolver does not model. | `DONE` | Composer now synthesizes source-level imports for fully qualified module references and expands alias-prefixed namespace chains like `partial_path::c::triple(...)` into nested synthetic targets before resolution. Verified with `moon test` on 2026-04-15 after adding the upstream `rusty_imports` parity case (`62/62` passing). |
| `WGSL-012` | Upstream parity gap | Upstream tests that depend on true Naga frontends/backends or runtime shader execution (`glsl_*`, validator error text, `raycast`, wgpu execution helpers) do not map directly to this source-level MoonBit port. | `DONE` | Source-level gaps were closed with native MoonBit coverage; true Naga-backed branches are covered by the pinned `tools/naga_oil_oracle`, including GLSL composition, upstream parse/validation diagnostics, additional-import override plugins, and ray-query validation. Runtime `wgpu` execution remains outside this library's scope. |
| `WGSL-013` | Architecture review | `Composer::default()` implicitly snapshots the package-level global WGSL registry, so instance-scoped composition and global helper APIs still form a hidden dual-state model. | `DONE` | Removed the package-level registry singleton and `Composer::from_registered_wgsl_source_registry`; `Composer::default()` is now the only state owner for composition. Re-verified with `moon test` on 2026-04-30 (`191/191` passing). |
| `WGSL-014` | Architecture review | `add_composable_module` validates imports against `module_sets` instead of the same registry-backed module view used by compose/export, so module bookkeeping and composition still do not share one availability graph. | `DONE` | `add_composable_module` now resolves imports through the Composer registry/module map and only uses composable-module metadata for shader-def inheritance. Verified with `moon test` on 2026-04-15 (`64/64` passing), including a new case where a composable module imports a plain registered source successfully. |
| `WGSL-015` | Architecture review | Legacy public compose/export entry points still expose `visited` / `modules` state from the old API model, leaving external mutable maps on the public control surface. | `DONE` | Removed the legacy stateful public compose/export entry points, added `Composer::compose_wgsl_source`, and migrated tests/docs to the `WgslComposeOptions` / `WgslExportOptions` surface. Verified with `moon fmt && moon info && moon test` on 2026-04-15 (`64/64` passing). |
| `WGSL-016` | Architecture review | `resolve_wgsl_source_with_path` remains a monolithic recursive pipeline that combines rewrite, condition evaluation, import planning, recursion, alias renaming, and final diagnostics in one function. | `DONE` | Extracted explicit compose helpers for source preparation, synthetic import seeding, import-request planning, import-request execution, and final output normalization, reducing the main recursive function to orchestration logic. Verified with `moon fmt && moon info && moon test` on 2026-04-15 (`64/64` passing). |
| `WGSL-017` | `moon ide analyze` architecture audit | Instance-scoped composition and package-level shared helpers still rely on raw registry maps instead of one explicit registry abstraction, so the shared registry boundary remains implicit in core code. | `DONE` | Kept `WgslSourceRegistry` as an explicit value object and deleted the package-level registry helpers/singleton; callers now use Composer-owned registries or explicit registry instances. Re-verified with `moon test` on 2026-04-30 (`191/191` passing). |
| `WGSL-018` | `moon ide analyze` architecture audit | WGSL declaration parsing and dependency analysis are split across composer and export, so item extraction, source catalogs, and tree-shaking can drift as the language surface grows. | `DONE` | Extracted shared declaration-graph and dependency-analysis logic into `wgsl_analysis.mbt`, and routed compose-time item extraction plus export-time tree-shaking/source-map generation through the same helpers. Verified with `moon fmt && moon test` on 2026-04-15 (`66/66` passing), including a new regression that compose/export preserve the same annotated global dependencies. |
| `WGSL-019` | `moon ide analyze` architecture audit | `composer.mbt` still owns most import-planning and namespace-rewrite helpers, leaving the primary orchestration file too large and too sticky for further features. | `DONE` | Composer code is now split by responsibility into core, registry, session, module API, import graph, pipeline, virtual override, and output-validation files. Re-verified with `moon test` on 2026-04-30 (`192/192` passing). |
| `WGSL-020` | GitHub issue #2 | Bevy-style const declarations with trailing line comments could be merged into the following declaration and then disappear from alias rewrites/tree-shaking. | `DONE` | Fixed by treating line-comment-terminated declarations as complete declaration boundaries. Released in `0.1.3` and verified with `moon test` on 2026-04-29. |
| `WGSL-021` | GitHub issue #3 | Mixed Bevy PBR import graphs exposed nested alias/type rewrite gaps in source-level composition. | `DONE` | Fixed alias/type rewrites and later removed the import-only entry preservation compatibility mode to match upstream `naga_oil` tree-shaking semantics. Verified with `moon test` on 2026-04-29. |
| `WGSL-022` | GitHub issue #4 | Repeated full-module imports under a nested alias skipped cached alias redirects, leaving expressions such as `view_bindings::view` unresolved. | `DONE` | Fixed by reusing cached alias redirects when a full-module import has already been visited. Released in `0.1.6`; verified with `moon test` on 2026-04-29 (`85/85` passing). |
| `WGSL-023` | Upstream naga_oil parity audit | The upstream compose test surface was only partially represented in local tests and not tracked as an explicit coverage matrix. | `DONE` | Added `docs/naga_oil-parity.md` with the upstream compose test/fixture matrix, source-level compatibility boundary, oracle-backed cases, and the next architecture priorities. |
| `WGSL-024` | Architecture review | Import syntax parsing/planning is still split between preprocessor metadata parsing and composer/module-graph import target parsing, so alias/group syntax can drift between APIs. | `DONE` | Consolidated metadata parsing and composer import-target planning around the same tokenizer-based import target parser, while keeping the existing public `collect_wgsl_import_targets` API. Verified with `moon test` on 2026-04-29. |
| `WGSL-025` | Upstream `additional_import` parity | Descriptor-level `additional_imports` exists, but source-level `compose_wgsl` / `export_wgsl_with_options` has no stable root-request API for injecting additional imports. | `DONE` | Added root-only `WgslComposeOptions.additional_imports`, wired descriptor-level additional imports into registered composable module source, inferred module names from source-tree file paths, and covered upstream additional-import override plugins. Verified with `moon test` on 2026-04-29. |
| `WGSL-026` | Upstream `bad_identifiers` parity | Naga can sanitize invalid/reserved identifiers during IR emission, but this source-level library currently lacks a defined identifier-sanitization layer. | `DONE` | Added AST-scoped identifier normalization for invalid/reserved top-level declaration names and function parameters, then aligned IR lowerer materialization for const-expression local lets, global/member reads, and temporary names so `bad_identifiers` is byte-gated against upstream. Invalid struct-member identifiers still report compose errors like upstream. Verified with `moon test ir` and `tools/check_moon_wgsl_byte_parity.sh` on 2026-05-09. |
| `WGSL-027` | Upstream `invalid_override` parity | Redirect/override-style source-level APIs do not yet diagnose redirects that never match a local declaration, while upstream catches invalid override cases through Naga. | `DONE` | Added export diagnostics for unmatched redirects and source-level support for upstream `virtual fn` / `override fn module::item`, including invalid non-virtual targets and transitive override stacks. Verified with `moon test` on 2026-04-29. |
| `WGSL-028` | Upstream `test_shader` parity | The local parity corpus lacks a simple compute shader smoke test corresponding to upstream `compute_test.wgsl`. | `DONE` | Added `testdata/upstream_compose/compute_test` and an export smoke test that preserves the compute entry point plus imported module dependency. Verified with `moon test` on 2026-04-29. |
| `WGSL-029` | GitHub issue #5 | `starts_with_text` slices by `prefix.length()`, which can abort on real Bevy WGSL containing non-ASCII source text during ifdef/import preprocessing. | `DONE` | Replaced prefix/suffix string slicing helpers with code-unit comparisons and added a Unicode source-line regression test for compose preprocessing. Verified with `moon test` on 2026-04-29. |
| `WGSL-030` | Oracle comparison follow-up | Descriptor-level `shader_defs` were not applied as upstream `effective_defs` while recursively composing imported modules, and `Bool(false)` shader defs were treated as disabled for `#ifdef`. | `DONE` | Composer now carries active shader defs through recursive imports, merges composable-module descriptor defs at the active module boundary, and treats present bool defs as defined for `#ifdef` while preserving their false value for `#if`. Verified with `moon test` on 2026-04-29. |
| `WGSL-031` | Upstream preprocessor parity | Composer-level `#if` handling used a permissive source-level evaluator that treated missing shader defs as `0`, accepted `1u` comparison literals, and ignored malformed `#else/#endif` scope structure. | `DONE` | Added a strict composer preprocessing evaluator for recursive composition, mirroring upstream errors for unknown shader defs, unknown operators, invalid comparison values, `#else` without condition, extra `#endif`, and missing `#endif`. Verified with `moon test` and `moon check --target all --deny-warn` on 2026-04-29. |
| `WGSL-032` | Upstream preprocessor parity | Root/local `#define` directives inside a composed shader source were parsed by metadata APIs but were not merged into composer active shader defs for the same source. | `DONE` | Composer now merges source-local `#define` metadata into active defs before strict scope evaluation and placeholder substitution, so root `#define` behaves like upstream `naga_oil` preprocessing. Verified with `moon test` on 2026-04-29. |
| `WGSL-033` | Preprocessing parity release | Known source-level preprocessing regressions from GitHub #2, #3, #5, #6, and #7 needed release/version guidance and public issue closure. | `DONE` | Released `Milky2018/moon_wgsl 0.5.0` for the historical #2/#3/#5/#6 fixes and the original #7 duplicate-binding/root-local define regressions; #7 later reported an additional `in` parameter regression tracked separately in `WGSL-035`. |
| `WGSL-034` | Oracle comparison follow-up | Oracle comparison existed as an ad hoc manual workflow rather than a reproducible project gate. | `DONE` | Added `tools/check_preprocess_parity.sh` and wired it into GitHub Actions. The script runs local preprocessing parity tests plus pinned `naga_oil` oracle diffs for simple compose, additional-import override compose, parser diagnostics, and validation diagnostics. Verified locally on 2026-04-30. |
| `WGSL-035` | GitHub issue #7 latest comment | Alias-scoped declaration-name rewriting treated function parameters as global declarations, so an imported Bevy entrypoint parameter such as `in` could be renamed in the signature while the function body still referenced `in`. | `DONE` | Added a failing aliased-module regression and fixed the root cause by separating global declaration-name collection from local declared identifiers. Verified with targeted tests and the preprocessing parity gate on 2026-04-30. |
| `WGSL-036` | Architecture refactor | Export still read Composer registry internals through public copy methods, and analysis/parser/ast exposed parser and rewrite helpers that were only implementation details. | `DONE` | Changed source-catalog export to consume `PreparedWgslSource`, deleted Composer registry copy/analyze APIs, moved compose-only list helpers into compose, and made unused analysis/parser/ast helper APIs private or removed. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-037` | Architecture refactor | Export still depended on preprocess to rebuild source catalogs, and parser still owned the old import-substitution preprocessor implementation. | `DONE` | Moved source-catalog construction into compose preparation and stored it on `PreparedWgslSource`, removed the export-to-preprocess dependency, and moved import substitution from syntax into private preprocess internals while preserving upstream parser parity coverage. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-038` | `moon ide analyze` architecture audit | Several stale helpers remained public after the package split, including unused default constructors, registry snapshots/copy APIs, tokenizer helpers, and white-box-only parser helpers. | `DONE` | Removed dead public helpers, moved `word_tail` into private preprocess code, hid top-level parser intermediates, and made `moon ide analyze` report no public values with zero production usage. Verified with `moon test` (`189/189`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-039` | Architecture refactor | Export still consumed `analysis` declaration-graph internals directly and exposed a redundant source-catalog facade after compose started preparing source catalogs. | `DONE` | Added an analysis-level tree-shaken emission result with generated ranges, rewired export to consume that stable result instead of `WgslDeclarationGraph`/`WgslGraphBlock`, deleted `export.build_wgsl_source_catalog`, and made the reachable-name walk private. Verified with `moon test` (`189/189`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` zero-public-unused check on 2026-04-30. |
| `WGSL-040` | Architecture refactor | Compose still used declaration graph internals for simple declaration-name/catalog enumeration, and parser/ast/analysis nodes remained unnecessarily `pub(all)` after downstream call sites no longer needed construction privileges. | `DONE` | Added `analysis.collect_wgsl_named_declarations`, rewired import alias maps, imported-item tracking, and prepared source catalogs to use it, and narrowed analysis emission/graph structs plus ast structs from `pub(all)` to `pub` where deny-warn allows it. Verified with `moon test` (`189/189`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` zero-public-unused check on 2026-04-30. |
| `WGSL-041` | Architecture refactor | Identifier sanitation and struct-member rejection still lived inside `compose`, even though they reuse analysis/parser/ast rather than composition state. | `DONE` | Moved identifier validation and normalization behind analysis-owned APIs, leaving `compose` as a thin adapter that maps invalid names to `ComposerError`. Verified with `moon test` (`189/189`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` zero-public-unused check on 2026-04-30. |
| `WGSL-042` | Architecture refactor | Virtual/override source rewrites still lived inside `compose`, making compose depend directly on declaration-graph internals for a finalization pass that does not need compose state. | `DONE` | Moved virtual/override handling behind analysis-owned APIs and left `compose` as a thin adapter that maps invalid override targets to `ComposerError`. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` on 2026-04-30. |
| `WGSL-043` | Architecture refactor | Resolver source registration depended on the whole `preprocess` package just to discover `#define_import_path`, coupling registry construction to conditional preprocessing and template substitution internals. | `DONE` | Added a dedicated `metadata` package for source-level module-path scanning and rewired resolver registration/diagnostics to use it directly. Also narrowed `PreprocessError` from `pub(all)` to `pub`. Verified with `moon test` (`191/191`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` on 2026-04-30. |
| `WGSL-044` | Architecture refactor | Full metadata extraction and import-use scanning still lived in `preprocess`, so compose and registry callers had to depend on the conditional-preprocessing package for read-only source metadata. | `DONE` | Moved directive extraction and import-substitution scanning ownership into `metadata`; removed the old preprocess implementation and migrated docs/tests to `@metadata`. Verified with `moon test` (`191/191`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` on 2026-04-30. |
| `WGSL-045` | Architecture refactor | Compose still owned pure import-planning and namespace-resolution helpers, mixing alias analysis/request identity logic with recursive compose-session execution. | `DONE` | Split import planning out of compose, then folded the remaining request scheduling back into compose after AST-driven semantic planning became the source of truth. Import alias/module target analysis now lives in `semantic`, not in a public planner package. Verified with `moon test` (`191/191`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` on 2026-04-30. |
| `WGSL-046` | Architecture refactor | Declaration dependency analysis still had a raw text-token fallback and function-local collection treated all locals as whole-function scope, so tree-shaking could keep member-access suffixes or drop globals shadowed only later/nested in a function. | `DONE` | Removed the raw dependency fallback, made expression references skip member/attribute access suffixes, and changed function dependency collection plus virtual override base detection to use statement-order active locals. Also deleted the now-unused public local-name-map API. Verified with `moon test` (`195/195`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-047` | Architecture refactor | Parser, AST, and analysis still exposed stale parser/debug surface (`starts_with_text`, source identifier collection, statement kind/text/span/local-name cache, and unused graph source/span fields) after downstream packages no longer needed them. | `DONE` | Deleted the stale public helpers/fields, moved source identifier collection into private import-planner code, and changed import stripping to iterate the parsed translation unit directly while preserving `#define_import_path` stripping semantics. Verified with `moon test` (`195/195`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-048` | GitHub issue #7 latest comment | Bevy PBR still failed on `let point_light = point_light(...)` because dependency analysis treated every identifier matching a local declaration in the same statement as local, so the RHS callee was tree-shaken away. | `DONE` | Replaced statement-wide local-name filtering with ordered, block-scoped local activation: simple declarations skip only the binding occurrence, compound-statement locals do not become active until their declaration statement ends, `for` header locals stay within the loop statement, and nested block locals are removed at `}`. Added regressions for same-name local initializer callees, type refs, `for` scopes, and nested block leakage. Verified with targeted analysis tests on 2026-04-30. |
| `WGSL-049` | Parser architecture refactor | The moonyacc-generated parser lived in a public `parser/generated` subpackage, exposing implementation-only parser tokens, token kinds, parse errors, and start functions as a stable API surface. | `DONE` | Merged the generated parser into the `parser` package and renamed the hand-written source tokenizer type to `WgslToken`. Later release validation showed that post-processing generated output into private internals made downstream build reproduction fragile, so the package now uses direct moonyacc `pre-build` generation and accepts the generated parser surface as a build-tool consequence. Verified with `moon check --target all --deny-warn`, `moon test`, `moon test --target js README.mbt.md`, `moon info`, and extracted-package publish validation on 2026-05-06. |
| `WGSL-050` | Parser architecture refactor | The `parser` package still exposed lexical/text preprocessing helpers (`tokenize`, comment stripping, line splitting, path splitting, brace balance), so downstream packages depended on parser internals instead of a dedicated WGSL lexical layer. | `DONE` | Split those helpers into a new `lex` package, moved tokenizer/comment/source-line white-box coverage with it, and rewired parser/metadata/preprocess/compose/analysis/resolver/import-planner/virtual-overrides imports so `parser` now only exposes parse facades. Verified with `moon test` (`216/216`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `moon info`, `tools/check_preprocess_parity.sh`, and `moon ide analyze` on 2026-04-30. |
| `WGSL-051` | AST/parser architecture refactor | Parser-only intermediate AST shapes (`WgslDeclHead`, `WgslDeclHeadKind`, `WgslRawTopLevelItem`, `WgslRawTopLevelKind`) and dead `WgslSpan` remained in the public `ast` package. | `DONE` | Moved declaration-head and raw-top-level parser intermediates into `parser` and deleted unused `WgslSpan`. After switching to direct moonyacc pre-build output, the generated public parser entry points require those parser-local intermediate result types to be public within the `parser` package API; fields remain implementation-owned. Verified with `moon check --target all --deny-warn`, `moon test`, `moon test --target js README.mbt.md`, and `moon info` on 2026-05-06. |
| `WGSL-052` | Oracle parity gate | The pinned `naga_oil` oracle gate compared only a narrow subset of deterministic upstream preprocessing outputs, leaving many covered local parity cases outside the CI oracle diff. | `DONE` | Expanded `tools/check_preprocess_parity.sh` to diff additional upstream oracle outputs and diagnostics for conditional missing imports, duplicate imports, duplicate struct imports, import-in-declaration, item imports, nested item imports, quoted duplicate imports, shared globals, imported entrypoint calls, invalid identifiers, and invalid override bases. Verified with `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-053` | AST API hardening | `WgslIdent`, the most widely reused AST leaf node, was publicly constructible with `pub(all)`, keeping the AST surface more mutable/forgeable than needed. | `DONE` | Changed `WgslIdent` to a read-only public struct with an explicit `WgslIdent::new` constructor, then rewired parser and AST internals away from direct record construction. Verified with `moon test` (`216/216`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-054` | AST API hardening | Remaining AST structs were still `pub(all)`, so external packages could forge parser-owned AST shapes directly instead of consuming read-only parse output. | `DONE` | Converted every public AST struct to read-only `pub struct` with explicit `::new` constructors, rewired handwritten parser code and moonyacc actions to construct through those APIs, and regenerated the private parser. `moon ide analyze` now reports no `pub(all) struct` in `ast`. Verified with `moon test` (`216/216`), `moon test README.mbt.md` (`11/11`), `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-055` | Oracle parity gate | The expanded oracle gate still left WGSL-scope upstream expected files implicit: missing-import and dual-source outputs were not diffed, and expected fixture inventory drift would not fail CI. | `DONE` | Added upstream oracle diffs for missing-import diagnostics and dual-source blending output, exposed `dual-source-blending` capability in the oracle CLI, and added an expected-fixture inventory assertion so uncovered upstream expected files must be explicitly classified. Verified with `tools/check_preprocess_parity.sh`, `cargo check --manifest-path tools/naga_oil_oracle/Cargo.toml`, and `moon check --target all --deny-warn` on 2026-04-30. |
| `WGSL-056` | Oracle parity gate | The coverage inventory still classified deterministic upstream GLSL and mixed GLSL/WGSL expected outputs as out-of-scope, even though the pinned oracle can compare those Naga-backed compose branches. | `DONE` | Added oracle diffs for `wgsl_call_glsl`, `glsl_call_wgsl`, `glsl_const_import`, `glsl_wgsl_const_import`, and `wgsl_glsl_const_import`, plus check-only oracle gates for `basic_glsl` and ray-query with `RAY_QUERY` enabled. Only upstream's `diagnostic_filters` stale expected writer output remains explicitly excluded because the upstream test itself is `should_panic`. Verified with `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-057` | Oracle parity gate | `diagnostic_filters` was documented as an upstream `should_panic` case, but the parity gate did not actively verify the pinned upstream failure mode. | `DONE` | Added a diagnostic-filter oracle failure gate that requires the current upstream invalid-call diagnostic instead of silently skipping the stale expected writer file. Verified with `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-058` | Source catalog parity | `PreparedWgslSource.source_catalog` still used the permissive source-catalog ifdef evaluator with only root bool/int defs, so descriptor-level module shader defs could diverge from the strict compose output, especially for upstream `Bool(false)` presence semantics. | `DONE` | Compose sessions now record full active `ShaderDefValue` maps per resolved source file and build source catalogs through the same strict ifdef/template path as composition. Added a regression where a composable module's `Bool(false)` descriptor def is still active for `#ifdef`, matching upstream. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-059` | Metadata strictness parity | Compose still consumed the forgiving metadata API, so `add_composable_module` could silently accept module-local `#define` directives that upstream rejects through `get_preprocessor_metadata(..., false)`. | `DONE` | Routed compose through strict metadata extraction with `allow_defines=false` for composable modules, while root/source composition continues to allow source-local defines. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-060` | Preprocess API cleanup | The old permissive `apply_wgsl_ifdefs` API remained public after compose/source-catalog paths moved to strict upstream semantics, leaving a non-naga conditional evaluator in the library surface. | `DONE` | Deleted the permissive evaluator and its private helpers so `preprocess` exposes only strict conditional filtering plus template substitution. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-061` | Preprocess parity | Source-level compose did not apply upstream `#version` preprocessing semantics, so invalid GLSL version directives were not rejected and accepted versions were not stripped from composed output. | `DONE` | Reused the preprocessor's `#version` parser in compose, added explicit `ComposerError::GlslInvalidVersion` / `InvalidWgslDirective` errors, and covered invalid plus accepted version directives in composer tests. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-062` | Analysis API hardening | `WgslGraphBlock` still exposed a stale `end` field even though all analysis/export/compose consumers use source text and generated offsets instead. | `DONE` | Removed the unused field from the analysis graph model and regenerated package metadata. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-063` | Core API hardening | Rename plans, composers, and source registries still exposed internal mutable implementation state as public fields, which made the library surface easier to forge and harder to evolve. | `DONE` | Made `WgslRenamePlan`, `Composer`, and `WgslSourceRegistry` opaque state objects by hiding internal rule arrays, maps, and refs behind existing constructors/methods. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-064` | Preprocess parity | `#version` validation lived only in the main compose line loop, so item-import extraction and source-catalog preparation could run strict ifdef filtering before the GLSL version directive was rejected or stripped. | `DONE` | Moved `#version` validation/stripping into the strict preprocessor path used by active-line scans, item imports, full imports, and source catalogs, then removed the duplicate compose-only validator. Added regressions for strict ifdefs and item-imported modules. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-065` | Metadata API parity | The public `get_preprocessor_metadata` API still swallowed parse errors and returned empty/default metadata, unlike upstream `naga_oil` metadata extraction. | `DONE` | Made `get_preprocessor_metadata` a strict raising API, removed the duplicate checked variant, updated compose call sites, and added invalid directive metadata coverage. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-066` | Metadata API cleanup | `get_preprocessor_data` remained as an old tuple convenience API that duplicated `PreprocessorMetaData` and had no production consumers. | `DONE` | Deleted the tuple helper and moved tests/docs to the canonical metadata object so metadata shape changes cannot drift across two public APIs. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-067` | Shader-def parity | Unsigned shader-def comparison parsed negative literals as signed ints and then reinterpreted them as `UInt`, while upstream `u32::parse` rejects negative values. Metadata also classified `#define X -0` as unsigned even though upstream falls through to signed parsing. | `DONE` | Rejected negative comparison values for `UInt` shader defs and made metadata choose the unsigned branch only for non-negative textual values. Added regressions for `1u`, `-1`, and `-0`. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-068` | Shader-def parity | `#if` comparison parsing ignored whitespace token boundaries, so `#if TEXTURE true` was parsed as shader def `TEXTUREtrue` instead of upstream's `TEXTURE` with an empty operator. `# version` was also accepted as a GLSL version directive even though upstream only matches literal `#version`. | `DONE` | Replaced tokenizer-collapsed `#if` expression parsing with a regex-shaped character scanner, made malformed `#if` tails fall through like upstream scope matching, and required `#version` to have no hash whitespace. Added regressions for operatorless comparisons, metadata effective defs, and spaced hash version lines. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-069` | Shader-def parity | `#ifdef` / `#ifndef` directive identifiers still used tokenizer-shaped parsing, so upstream-valid shader-def names beginning with a digit or `|` were rejected even though naga_oil's regex accepts them. | `DONE` | Replaced directive identifier parsing in preprocess and metadata with the same regex-shaped shader-def name scanner used by `#if` expressions, preserving upstream one-token capture for trailing text. Added regressions for leading digit and leading pipe names. Verified with targeted tests and full preprocessing parity gates on 2026-04-30. |
| `WGSL-070` | Shader-def parity | Token-based shader-def reference and `#define` value parsing still dropped `_` and non-ASCII word characters when they appeared after a digit token, so names such as `1_NAME` and malformed values such as `1_name` diverged from naga_oil's regex capture. | `DONE` | Reused the regex-shaped shader-def name character predicate for token-to-text conversion in preprocess and metadata, including underscore and non-ASCII code units after digit-start tokens. Added regressions for digit-leading underscore names and malformed underscore values. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-071` | WGSL directive parity | WGSL directive-section extraction treated block-comment-only lines as empty after comment replacement, while naga_oil only preserves empty lines and `//` lines before ending the directive section. | `DONE` | Changed directive-section scanning to follow upstream's raw trimmed-line check, so `/* ... */` terminates the directive section and later `enable`/`requires`/`diagnostic` lines remain source text. Added a regression for block comments before `enable`. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-072` | WGSL directive parity | `enable` / `requires` extension extraction tokenized directive bodies into WGSL identifiers, while naga_oil captures `[^;]+` and splits only on commas. Bodies such as `foo-bar` or `one two` were therefore split incorrectly. | `DONE` | Replaced tokenizer-based extension extraction with comma splitting plus trimming, preserving upstream extension text exactly except empty parts. Added regressions for hyphenated and space-containing extension entries. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-073` | WGSL directive parity | `diagnostic(...)` extraction tokenized severity and rule as WGSL identifiers, while naga_oil captures raw `[^,]+` and `[^)]+` text before trimming. Hyphenated or space-containing rules were silently left in source instead of being extracted. | `DONE` | Replaced token matching with a regex-shaped scanner that preserves raw diagnostic rule text and still validates severity against upstream's allowed set. Added a regression for hyphenated/space-containing rule text. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-074` | Preprocess directive parity | Keyword-only `#import`, `#define`, and `#define_import_path` lines were treated as directives by the generic parser, but naga_oil's regexes require whitespace after those keywords before they match. This caused local preprocessing to remove source lines or raise module-define errors that upstream would not. | `DONE` | Added regex-shaped required-whitespace directive matching for import/define/define_import_path in preprocess and metadata paths, leaving keyword-only lines as normal source/effective shader-def references. Added regressions for all three keyword-only forms. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-075` | Metadata parity | `#define_import_path` parsing stripped trailing semicolons from the captured path token, while naga_oil's `([^\s]+)` capture keeps semicolons and any other non-whitespace bytes inside the first token. | `DONE` | Removed semicolon trimming from both source-path discovery and full preprocessor metadata extraction. Updated regressions so `#define_import_path demo::module;` yields `demo::module;`. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-076` | Shader-def parity | Unsigned shader-def metadata and `#if` comparison parsing used signed integer parsing plus reinterpretation, while naga_oil uses `u32::parse`. Full-range values above signed `i32::MAX` could be rejected or misclassified. | `DONE` | Switched unsigned define parsing and unsigned comparison parsing to `parse_uint`, preserving signed fallback for negative values. Added a regression for `4000000000`. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-077` | Shader-def replacement parity | Shader-def substitution replaced the specific matched reference, while naga_oil loops over original captures but calls `Regex::replace` on the current output, replacing the first remaining reference of that form. Undefined references before defined references could therefore be replaced upstream but not locally. | `DONE` | Reworked line substitution to mirror upstream's first-match replacement loop separately for plain `#NAME` and delimited `#{NAME}` references. Added a regression where earlier undefined references receive later defined values. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-078` | Shader-def parity | Delimited shader-def references `#{...}` trimmed and accepted arbitrary body text locally, while naga_oil only matches `#\s*\{([\w|\d|_]+)\}` with a non-empty regex-name body immediately followed by `}`. | `DONE` | Tightened delimited shader-def parsing in preprocess and metadata to require the whole body to match the shader-def name character class, without trimming. Added regressions for spaces/`+` inside braces remaining literal. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-079` | Conditional directive parity | Conditional directive matching accepted `#ifdef|FLAG` and `#else ifdef|FLAG` because generic directive tails did not require whitespace after `if`/`ifdef`/`ifndef`, while naga_oil's regexes require `\s+` after those keywords. | `DONE` | Reused required-whitespace directive/word-tail scanners for direct and `#else if*` conditional forms in preprocess and metadata. Added regressions for no-whitespace direct and else-prefixed forms. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-080` | WGSL directive parity | Directive-section termination used exact directive keywords locally, while naga_oil keeps the directive section open for any line whose trimmed text starts with `enable`, `requires`, or `diagnostic`, even when the line is not a valid directive. | `DONE` | Changed directive-section state tracking to use upstream prefix checks and updated the regression so `enable_extra();` still allows a later `enable f16;` to be extracted. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-081` | Regex whitespace directive parity | `enable`/`requires` accepted punctuation immediately after the keyword, while naga_oil requires `\s+`; directive scanners also treated only space/tab as regex whitespace in several `#\s*` and `\s*` positions. | `DONE` | Added regex-whitespace scanners/trimmers across preprocess, metadata, and directive parser paths; required `enable`/`requires` body separators to match upstream `\s+`, including the single-space empty-body non-match and repeated-whitespace empty-body error. Added regressions for punctuation separators and form-feed whitespace. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-082` | Unicode shader-def word parity | Shader-def scanners treated every non-ASCII UTF-16 code unit as a regex word character, while naga_oil's Rust regex `\w` rejects many non-word scalars such as emoji but accepts Unicode letters/marks and supplementary-plane word characters as full scalar values. | `DONE` | Added a shared lex-level Rust-regex word range table generated from naga_oil's regex dependency and rewired preprocess/metadata shader-def scanners to consume Unicode scalar widths instead of tokenizer identifiers. Added regressions for emoji suffixes, delimited emoji bodies, Unicode words, and `#ifdef FLAG💡` prefix capture. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-083` | Unicode regex whitespace parity | The directive scanners still modeled Rust regex `\s` as ASCII whitespace only, so NBSP, U+2000-series spaces, and ideographic space were kept inside directive bodies or blocked `#\s*` / `\s+` matches that naga_oil accepts. | `DONE` | Added a shared lex-level Rust-regex whitespace predicate/trimmer and rewired preprocess, metadata, module-path discovery, and directive parser paths to use it. Added regressions for NBSP, EM SPACE, and ideographic space in `#ifdef`, shader-def replacement, `#define_import_path`, `enable`, `requires`, `diagnostic`, and parser directive tails. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-084` | AST/parser WGSL blankspace parity | The general WGSL tokenizer and AST type-fragment parser treated only space/tab/CR/LF as blankspace, missing Pattern_White_Space code points required by the WGSL specification such as vertical tab, form feed, next line, left/right marks, and paragraph separator. | `DONE` | Added a separate lex-level WGSL blankspace predicate/trimmer distinct from Rust regex `\s`, updated tokenizer identifier boundaries and AST/parser fragment trimming/type-ref tokenization to use it, and added regressions for form feed, left-to-right mark, and paragraph separator. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-085` | Import preprocessing parity | The same tokenizer was being used for both naga_oil import preprocessing and WGSL AST parsing, even though naga_oil's import tokenizer uses Rust whitespace while WGSL parsing uses Pattern_White_Space. Multi-line import collection also used token brace balance, ignoring braces inside quoted import paths, while upstream counts raw braces per line with saturating close handling. | `DONE` | Split `tokenize_wgsl` from the naga_oil-compatible `tokenize`, kept import parsing/substitution on Rust whitespace, moved AST parsing to WGSL blankspace, and added raw naga_oil import brace counting for preprocess/metadata collection. Added regressions for NBSP import tokenization and quoted-brace multiline import errors. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-086` | Import tokenizer parity | The naga_oil-compatible tokenizer still accepted every non-ASCII code unit as an identifier character, while upstream uses `unicode_ident::is_xid_start` / `is_xid_continue`; emoji and other non-XID scalars could therefore be substituted as identifiers locally. | `DONE` | Added generated XID start/continue range tables from Rust `unicode-ident`, rewired the naga_oil tokenizer mode to use them, and kept WGSL tokenizer mode separate for underscore and Pattern_White_Space handling. Added a regression proving emoji no longer joins adjacent Unicode identifiers. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-087` | Non-ASCII source width parity | Comment replacement and removed-line padding used MoonBit code-unit length, while naga_oil uses Rust byte offsets and `str::len()`. Non-ASCII comments or removed preprocessor lines therefore produced shorter padding than upstream and could shift preprocessing output. | `DONE` | Split parser-safe comment replacement from naga_oil byte-width comment replacement, added UTF-8 byte-length padding for preprocess/metadata offsets and removed-line output, and kept AST parsing on code-unit-preserving comments. Added regressions for Chinese and non-BMP comments plus non-ASCII removed directives. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-088` | Import diagnostic offset parity | Import parsing and substitution added MoonBit code-unit token positions to byte-width source offsets, while naga_oil reports UTF-8 byte offsets from its tokenizer. Diagnostics and recorded import offsets could drift after non-ASCII source prefixes. | `DONE` | Converted import-parser token positions and substitution token positions to UTF-8 byte offsets at the metadata/parser boundary, while leaving AST parser token positions code-unit based for safe MoonBit slicing. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-089` | Remaining import collection/directive dispatch parity | Direct `metadata.parse_imports` still used token-aware brace balancing and compose source preprocessing dispatched directives with `String.trim()`, diverging from naga_oil's raw import brace counting and Rust regex whitespace behavior. Quoted braces and Unicode whitespace prefixes could therefore take different paths outside the main preprocess pipeline. | `DONE` | Rewired direct import parsing to `naga_oil_raw_import_brace_balance_next` and made compose directive dispatch use the shared naga_oil regex whitespace trim. Added regressions for raw-brace import block collection and Unicode-whitespace compose import directives. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-090` | Import target empty-whitespace parity | `parse_wgsl_import_targets` still used MoonBit `String.trim()` for the empty-input fast path, so Unicode regex whitespace could bypass the intended empty import-path diagnostic and fall into parser EOF handling. | `DONE` | Switched the empty-input guard to shared naga_oil regex whitespace trimming and reported the UTF-8 byte length. Added a parser regression for NBSP-only import target input. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-091` | Delimited shader-def regex parity | Template constant substitution trimmed the contents of `#{...}` before matching, while naga_oil's delimited shader-def regex captures only a contiguous `[\w|\d|_]+` body. Inputs such as `#{ KNOWN }` were incorrectly substituted locally. | `DONE` | Removed body trimming from the delimited template-const replacement path and required the raw body to match the shared regex word class, matching metadata/effective-def collection. Extended the delimited shader-def regression to keep spaced bodies untouched. Verified with `moon test`, `moon test README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-04-30. |
| `WGSL-092` | Post-refactor lex surface cleanup | The old token-aware import brace helper remained public after import collection moved to naga_oil raw brace counting, and internal WGSL blankspace scan helpers were still exported. This kept a misleading API path open for future preprocessing code. | `DONE` | Deleted the unused token-aware brace helper and made WGSL blankspace width/skip helpers package-private, leaving only the parser-facing trim/code-point APIs public. Verified with `moon check --deny-warn`, `moon info`, and `moon test lex` on 2026-04-30. |
| `WGSL-093` | Rust `str::lines` parity | The shared `source_lines` helper stripped a trailing carriage return from every split line and returned one empty line for empty input, while Rust `str::lines()` strips `\r` only as part of `\r\n` and returns no lines for `""`. This could skew preprocessing line iteration and offsets for CR-only or empty sources. | `DONE` | Reimplemented `source_lines` with explicit `\n` scanning and `\r\n` handling, preserving lone `\r` fragments and omitting the synthetic empty line for empty input. Added lex regressions for empty, CRLF, and lone-CR cases. Verified with `moon test lex`, `moon test preprocess_test.mbt`, and `moon check --deny-warn` on 2026-04-30. |
| `WGSL-094` | Shader-def replacement offset parity | Preprocess ordinary-line offset advancement still used the raw original line width, while naga_oil advances by the shader-def-replaced `original_line.len()`. Diagnostics after a length-changing shader-def substitution could therefore point before or after the upstream byte position. | `DONE` | Advanced offsets by the UTF-8 byte width of the replaced original line and added a regression where a length-changing shader-def line precedes an ambiguous import diagnostic. Verified with targeted preprocess tests on 2026-04-30. |
| `WGSL-095` | Direct import parser empty-input parity | Direct `metadata.parse_imports` skipped empty/whitespace-only input and returned success, while upstream `parse_imports` reports `expected #import` at the input byte length when the tokenizer has no first token. | `DONE` | Tracked whether any import was parsed and raised the upstream empty-input diagnostic when none were found. Added regressions for empty and NBSP-only inputs. Verified with targeted metadata tests on 2026-04-30. |
| `WGSL-096` | Import semicolon diagnostic parity | The moonyacc import grammar accepted repeated trailing semicolons and reported a generic parser error for tokens after `;`, while upstream accepts only a final `;` and reports `unexpected token after ';'` at the following token position. | `DONE` | Added a token-stream precheck before the generated import parser and removed the now-unreachable catch fallback. Added parser regressions for `a; b` and `a;;`. Verified with parser, metadata, preprocess, and deny-warn checks on 2026-04-30. |
| `WGSL-097` | Import `as` token parity | The generated import-parser bridge treated every `as` identifier as an alias keyword, while naga_oil's hand-written parser treats `as` as a normal identifier unless it follows a parsed import path. Imports of items named `as` and aliases named `as` were rejected locally. | `DONE` | Made the import-token bridge context-sensitive so `as` becomes AS only after a path identifier, and added regressions for `#import as` and `#import a as as`. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-098` | Import group-open parity | The generated import grammar accepted grouped imports after bare identifiers, such as `#import a {b}` and `#import a b {c}`, while naga_oil reports `open brace must follow \`::\`` unless the current path token ends in `::`. | `DONE` | Added token-stream validation before the generated import parser so group opens require a previous `::` path and `as` aliases require a following identifier. Added exact malformed-import regressions. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-099` | Deprecated item-list prefix parity | The generated import grammar normalized deprecated item-list prefixes that already ended with `::`, so `#import a:: b` became `a::b`; naga_oil mechanically pushes `format!("{current}::")`, yielding `a::::b`. | `DONE` | Changed the moonyacc deprecated item-list action to always append `::` before target paths and added an exact regression for `#import a:: b`. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-100` | Naga_oil numeric tokenization parity | The shared `tokenize` implementation skipped numeric literal suffixes as non-identifiers, but naga_oil's tokenizer treats digits as `Other` and suffix letters such as `u`, `e`, and `f` as identifiers. This could miss import substitution inside numeric-looking tokens. | `DONE` | Restricted numeric-literal grouping to `tokenize_wgsl` and restored naga_oil-compatible digit handling for `tokenize`. Added tokenizer and substitution regressions for numeric suffix identifiers. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-101` | Non-BMP tokenizer parity | The tokenizer used UTF-16 code units for XID and `Other` classification, so non-BMP XID characters were split and non-BMP non-identifiers became surrogate `Other` tokens. Naga_oil iterates Rust `char_indices`, classifying each Unicode scalar value once. | `DONE` | Reworked tokenizer iteration to use `wgsl_code_point_at`, kept code-unit offsets for local slicing, and updated `Other` token widths. Added regressions for emoji `Other` tokenization and U+10400 XID imports. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-102` | Comment stripper CRLF parity | `replace_comments_naga_oil` split on raw `\n`, so CRLF comment lines counted the `\r` as comment text and emitted one extra padding space. Naga_oil drives comment stripping from Rust `str::lines()`, which strips the CR in `\r\n`. | `DONE` | Changed the naga_oil comment-strip path to use `source_lines` while leaving the parser-safe code-unit stripper unchanged. Added line and block comment CRLF regressions. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-103` | Diagnostic directive empty-field parity | The diagnostic directive parser skipped whitespace before checking field length, so `diagnostic( , rule);` and `diagnostic(off, );` failed to match. Naga_oil's regex can backtrack and capture one whitespace character, then trim it to an empty severity/rule. | `DONE` | Reworked diagnostic field scanning to preserve regex-style whitespace-only captures while still rejecting truly empty fields. Added regressions for empty severity diagnostics and empty rule captures. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-104` | Direct import parser token-stream parity | Direct `metadata.parse_imports` still line-collected import blocks, so it could parse multiple `#import` directives in one call and reported CRLF tail offsets from normalized lines instead of matching upstream's single tokenizer stream. | `DONE` | Rewrote direct import parsing to validate the leading `# import` tokens with the naga_oil tokenizer and parse the entire remaining source tail as one import target stream. Added regressions for spaced `# import`, empty import tails, deprecated newline item-list parsing, duplicate `#import` rejection, and CRLF byte offsets. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-105` | Compose import directive dispatch parity | The source-level compose path used the generic directive parser for `#import`, `#define`, and `#define_import_path`, so keyword-only forms could be handled as directives even though upstream regexes require tail whitespace. It also collected import targets through a resolver helper that swallowed parser errors. | `DONE` | Added compose-local required-whitespace directive matching and changed active import request construction to strictly parse import specs, while still accepting upstream-empty imports and a single final semicolon. Added composer regressions for keyword-only directive dispatch, invalid import specs, and empty semicolon imports. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-106` | Import parser non-BMP `Other` span parity | The generated import-token bridge still measured `Other` tokens as one UTF-16 code unit even after the naga_oil tokenizer moved to Unicode scalar tokens, so non-BMP invalid tokens could be sliced through the middle of a surrogate pair before reporting diagnostics. It also reported ordinary `unexpected token` for invalid tokens after `;`, while upstream reports `unexpected token after ';'` for any following token. | `DONE` | Reused `WgslToken::end()` for generated import-token spans and moved semicolon-tail checking before token conversion. Added parser regressions for non-BMP invalid tokens before and after a semicolon. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-107` | Compose comment-stripped directive parity | Source-level compose dispatched `#import`, `#define`, and conditional directives from original source lines, while upstream `Preprocessor::preprocess` dispatches from comment-replaced lines zipped with original lines. Block-comment-prefixed directives such as `/* x */#import demo::item` could therefore remain as ordinary source locally. | `DONE` | Changed compose preprocessing to carry original and naga_oil comment-stripped line text together, using stripped lines for directive recognition and raw import brace counting while preserving original lines in emitted source. Added a composer regression for block-comment-prefixed imports. Verified with moon test, moon test README.mbt.md, moon check --target all --deny-warn, moon info, moon ide analyze, and tools/check_preprocess_parity.sh on 2026-04-30. |
| `WGSL-108` | Architecture risk review | Semantic rewrite still used source-span copy-and-replace in rename and virtual override paths, so identifier correctness depended on raw source slicing even after AST analysis found the nodes. | `DONE` | Replaced semantic source splices with AST-scoped token rewrites inside `analysis`; identifier normalization now uses the same `WgslRenamePlan` flow. Verified with `moon test --target js`, `moon test --target js README.mbt.md`, `moon check --target all --deny-warn`, `moon info`, `moon ide analyze`, and `tools/check_preprocess_parity.sh` on 2026-05-05. |
| `WGSL-109` | Architecture risk review | Preprocess and compose still have necessary source-retaining passthrough emission for naga_oil parity, but those paths are not documented as an explicit boundary and can be mistaken for semantic rewrite logic. | `DONE` | Centralized preprocessing and compose passthrough output behind typed trivia emission kinds (`WgslPreprocessEmissionKind`, `WgslComposeSourceEmissionKind`) so raw `original_text`/blanking behavior is isolated from semantic rewrite packages. Verified with `moon test --target js` on 2026-05-05. |
| `WGSL-110` | Architecture risk review | Source maps and catalogs now use final prepared WGSL, which fixed runtime alias leakage, but precise mapping back to original dependency files requires a separate origin graph instead of raw source catalogs. | `DONE` | Added `WgslSourceOriginEntry` and `PreparedWgslSource.source_origins`, recorded declaration origins during AST-driven compose/import extraction, and changed export source maps to use the origin graph while keeping `source_catalog` tied to final prepared WGSL. Verified with `moon test --target js` on 2026-05-05. |
| `WGSL-111` | GitHub issue #8 latest comment | `moon_wgsl 0.7.2` still failed mgstudio's real Bevy PBR fragment path with `failed to resolve shader import: view_bindings::clustered_lights`. The missing case was a cached qualified alias import: one `mesh_view_bindings as view_bindings` import emitted the declarations, while a later module reused only cached redirects, and the live-redirect filter dropped them because the target declarations were not local to that later module. | `DONE` | Added a dedicated `CachedQualifiedAliasReference` scope and kept cached alias redirects when the current AST references the alias path. Added regressions for real `bevy_pbr/src/render/pbr.wgsl` and address-of alias member references. Verified with `moon test --target js`, `moon test --target js README.mbt.md`, `moon check --target all --deny-warn`, and `tools/check_preprocess_parity.sh` on 2026-05-05. |
| `WGSL-112` | Architecture risk review | The standalone final text-emission package remained as a compose boundary, which made identifier policy look like a post-hoc text write instead of semantic analysis. | `DONE` | Deleted that package, moved identifier policy into `analysis/wgsl_identifier_policy.mbt`, replaced compose finalization with output validation, and removed direct validation-tool use of token-emission APIs. Verified with `moon check --target all --deny-warn`, targeted parity tests, and `tools/check_wgsl_validation.sh` on 2026-05-05. |
| `WGSL-113` | Architecture risk review | The separate virtual-override package still exposed span-token emission helpers through `analysis`, leaving a public escape hatch outside the semantic-analysis package. | `DONE` | Folded virtual overrides into `analysis`, deleted the separate package, made canonical token edits private implementation details, and updated validation/tests/docs to avoid that API. Verification is tracked with this refactor. |
| `WGSL-114` | Architecture risk review | Analysis tree-shaking, module-item extraction, duplicate declaration cleanup, duplicate binding cleanup, and virtual override emission still appended parsed declaration `block.text` directly. | `DONE` | Added an analysis-owned canonical top-level block emitter and routed semantic declaration output through it. Raw declaration text is now only parser input for graph construction and origin spans, not the output path for analysis transforms. Verified with targeted analysis/compose/upstream/Bevy/export tests and the WGSL validation gate on 2026-05-05. |
| `WGSL-115` | Architecture risk review | Preprocess and compose passthrough emission decisions were still embedded in the main orchestration loops, making trivia retention look like semantic source rewriting. | `DONE` | Renamed passthrough actions to explicit trivia emission kinds and extracted per-item preprocess/compose dispatch helpers. Main pipelines now orchestrate state while scoped helpers own blanking, trivia retention, source-item rewriting, and import side effects. Verification is tracked with this refactor. |
| `WGSL-116` | Architecture risk review | The `analysis` package still exported `WgslGraphBlock`, `WgslDeclarationGraph`, and raw graph builders, leaving an external API path that could consume declaration `text` directly instead of the canonical emission APIs. | `DONE` | Made the declaration graph and graph builder functions private implementation details. Public callers now use emitted-declaration, tree-shaken-source, module-extraction, normalization, or rewrite APIs, which centralize canonical declaration emission inside `analysis`. Verified with `moon check --target all --deny-warn` and `moon info` on 2026-05-05. |
| `WGSL-117` | Architecture risk review | The `semantic` package still exposed debug-style import/reference graph APIs and construction-only fields, making semantic internals look like a supported public graph surface. | `DONE` | Deleted the unused semantic import graph model and builder, privatized reference-path collection helpers, narrowed semantic result structs from `pub(all)` to `pub`, removed unused import-plan fields, and stopped advertising `@semantic` as a user-facing README entry point. Verified with `moon check --target all --deny-warn` on 2026-05-05. |
| `WGSL-118` | Analysis API hardening | `analysis` still exported non-strict declaration extraction wrappers, raw module-item string extraction, and a generic rename-plan API that let downstream code choose internal rename modes directly. | `DONE` | Removed unused non-strict analysis wrappers and raw string extraction, routed origin recording through strict module-item origins, converted tests to the canonical tree-shaken-source result, and replaced the generic public rename-plan builder with explicit global/reference plan builders while keeping rule kinds and manual plan mutation private. Verified with `moon check --target all --deny-warn` on 2026-05-05. |
| `WGSL-119` | Lexer/parser API hardening | `lex` and `parser` still exported unused token/debug accessors, regex/source-line internals, and directive-line micro-parsers that encouraged consumers to bypass the scan/AST parser boundaries. | `DONE` | Deleted unused token text/operator/punctuation accessors, privatized lexer helper functions, removed directive-prefix grammar support and regenerated the private moonyacc parser, and made single-line directive/source-item offset helpers package-private. Verified with `moon check --target all --deny-warn` on 2026-05-05. |
| `WGSL-120` | Common API hardening | Generated result records in `common` were still `pub(all)`, allowing downstream code to forge prepared sources, metadata outputs, diagnostics, source maps, and resolved modules instead of receiving them from the owning pipeline. | `DONE` | Converted generated result records to read-only public structs with explicit constructors, and migrated resolver, metadata, preprocess, compose, semantic, and export call sites to the constructors. User input records such as source files, options, redirects, and import definitions remain constructible. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-121` | Common API hardening | `ComposableModuleDefinition` was still externally constructible even though it is a Composer-owned registration result, and diagnostics still advertised an unused `Info` severity. | `DONE` | Made `ComposableModuleDefinition` read-only with an explicit constructor used by compose, and removed the unused `Info` diagnostic severity so diagnostics only expose generated severities. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-122` | Canonical emission hardening | Analysis and metadata each owned separate identifier rewrite emitters; metadata still performed import substitution with local source-span splice logic instead of the shared token writer. | `DONE` | Promoted the canonical WGSL token writer into the lexical layer, rewired analysis virtual/rename emission and metadata import substitution to the shared writer, and deleted the duplicated analysis-private token writer. Parser no longer exposes token-emission APIs. Verified with `moon test` and the parity/validation gates on 2026-05-06. |
| `WGSL-123` | Parser API hardening | The shared canonical token writer briefly lived in `parser`, making low-level token-edit emission look like part of the parser facade. | `DONE` | Moved canonical emission to `lex`, leaving `parser` as parse/scan APIs only. Verified with `moon check --target all --deny-warn` and `moon ide analyze` on 2026-05-06. |
| `WGSL-124` | AST API hardening | Several AST fields still carried unused source-preserving spans or raw text (`WgslDirective.text`, attribute name spans, local initializer text, statement spans, type-ref end, and function-argument type text) after semantic analysis moved to node refs and canonical token emission. | `DONE` | Removed the unused AST fields, tightened constructors, regenerated the moonyacc parser, and updated parser white-box expectations to the smaller AST shape. Verified with `moon check --target all --deny-warn` and `moon test` on 2026-05-06. |
| `WGSL-125` | Parser scan DTO hardening | Directive/source-directive scan items still exposed unused end/start spans from the old source-preserving scanner model. | `DONE` | Removed unused scan DTO span fields and simplified source-directive item construction to line/kind only. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-126` | Internal state hardening | Shared token edits and declared import bindings were opaque in practice but still exposed readable fields, allowing downstream code to couple to internal rewrite/import-collection layout. | `DONE` | Marked `WgslCanonicalTokenEdit` and `DeclaredImportBinding` fields private while preserving constructor/array-passing APIs needed across package boundaries. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-127` | Common API hardening | Public output records still carried stale provenance/configuration fields from earlier source-preserving writeback paths, including import offsets, directive line locations, prepared-source define snapshots, diagnostic spans, source-map spans, and composable-module cached source/import copies. | `DONE` | Removed those unused output fields and narrowed descriptor tests to the remaining semantic registration state. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-128` | Parser/metadata boundary hardening | Metadata-only source-directive scanning was exported from `parser`, and expression AST nodes still cached expression source text for path collection. Both kept source-preserving details visible at the wrong abstraction layer. | `DONE` | Moved source-directive-section scanning into `metadata`, removed the parser export, and replaced `WgslExprNode.text` with structural path-name reconstruction for qualified identifier collection. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-129` | Compose phase-order hardening | Compose preparation still used a non-strict import-stripping helper, and the first strict replacement attempted to parse invalid WGSL bodies before missing imports were resolved. | `DONE` | Deleted the non-strict strip API, kept import planning before body validation by feeding semantic planning the active AST source directly, and preserved strict stripping only for source-catalog generation. Verified with `moon check --target all --deny-warn`, targeted upstream missing-import parity, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-130` | Package boundary hardening | The standalone `semantic` package still exposed import-planning and reachability internals as a public package even though all production callers were `compose` and `analysis`. | `DONE` | Removed the `semantic` package, moved import/reference planning into `compose`, moved declaration reachability into `analysis`, and kept the moved compose planning records private. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-131` | Parser strictness hardening | `parser` still exported a non-strict translation-unit parser that preserved unparsed top-level fragments as raw declarations, leaving a public fallback path around the strict AST parser. | `DONE` | Removed the non-strict translation-unit parser and lenient top-level reduction path; parser white-box tests now use the strict parser through a local test helper. Verified with `moon check --target all --deny-warn`, parser targeted tests, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-132` | Source catalog hardening | `WgslSourceCatalogEntry` still cached declaration source text, and export source-map fallback used text equality to disambiguate same-name declarations. | `DONE` | Removed cached source text from catalog entries and changed export fallback matching to use parser-generated declaration spans. Verified with `moon check --target all --deny-warn`, targeted Bevy/export tests, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-133` | Analysis rewrite API hardening | `analysis` still exported `WgslRenamePlan` and plan builders, allowing callers to couple to internal rewrite rule composition. | `DONE` | Hid rename plans and plan builders, and exposed only intent-level global-declaration and reference rewrite functions for cross-package callers. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-134` | Directive scan API hardening | `parser.WgslDirectiveScanItem` exposed raw original/cleaned text, start offsets, kind, and tail-space flags as public fields, letting downstream code forge scanner state and treat naga_oil trivia as a general text DTO. | `DONE` | Made directive scan items opaque and routed cross-package consumers through named accessors for scanner kind, offsets, trivia, and cleaned directive text. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-135` | Virtual override API hardening | `analysis` still exposed `apply_wgsl_virtual_overrides_raw` and a mutable-looking output DTO, so `compose` had to inspect analysis internals to distinguish invalid overrides from successful rewrites. | `DONE` | Replaced the raw output API with `apply_wgsl_virtual_overrides`, returning rewritten source directly and raising `WgslVirtualOverrideError` for invalid overrides or parse failures. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-136` | Preprocess scan DTO hardening | Shader-def, template-const, and comparison scan records still exposed parser spans and token text as public fields, so preprocessing code could couple to parser storage layout instead of named scanner semantics. | `DONE` | Made `WgslShaderDefReference`, `WgslTemplateConstReference`, and `WgslShaderDefComparison` opaque and moved callers/tests to named accessors. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-137` | Import substitution state hardening | `metadata` still exposed `DeclaredImportBinding`, `parse_imports`, and `substitute_identifiers`, forcing `preprocess` to share raw mutable import-binding arrays and call low-level token substitution directly. | `DONE` | Hid declared import bindings and replaced the raw parse/substitute API with opaque `WgslImportSubstitutionState` methods for declaring import blocks and rewriting active WGSL source. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-138` | Canonical writer API hardening | `lex` still exposed `WgslCanonicalTokenEdit` and a batch edit writer, so semantic packages could share the writer's internal edit representation directly. | `DONE` | First hid token edit records behind an opaque rewriter, then deleted the obsolete source-preserving writer after analysis and metadata moved to AST-collected spans plus canonical token-stream emitters. Verified with the full local gate on 2026-05-06. |
| `WGSL-139` | Lexer surface hardening | The naga_oil raw import brace counter was still exposed from `lex` even though it is parser-specific import collection behavior. | `DONE` | Moved raw import brace counting into the parser package and removed the helper from the public lexer interface. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-140` | Preprocessor API pruning | `Preprocessor::extract_wgsl_directives` remained as a public compatibility method even though production callers use full preprocessing or metadata extraction. | `DONE` | Removed the compatibility directive-extraction method, moved its direct error tests to metadata, and deleted the now-unused preprocess error variants. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-141` | Metadata API pruning | `metadata.extract_wgsl_directives` became an unused public half-pipeline after the preprocessor compatibility method was removed. | `DONE` | Made directive extraction private to metadata so callers must use `get_preprocessor_metadata` for the complete metadata pipeline. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-142` | Composer result ownership hardening | `ComposableModuleDefinition` was still defined in `common`, so external callers could construct a Composer registration result and couple to its storage layout instead of receiving it from `Composer::add_composable_module`. Resolved import modules also exposed raw resolver fields across package boundaries. | `DONE` | Moved `ComposableModuleDefinition` into `compose`, made its constructor package-private, exposed only read-only semantic query methods, and made resolved import modules opaque with named accessors. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-143` | Analysis result API hardening | Analysis result records (`WgslTreeShakenSource`, emitted declarations, and module-origin extraction) still exposed generated text, span, and origin arrays as public fields. Cross-package compose/export callers could therefore couple to analysis storage instead of using the canonical analysis facade. | `DONE` | Made analysis result records opaque and moved compose/export to read-only accessor methods that return copied arrays for declaration and origin lists. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-144` | Expression AST source-cache hardening | `WgslExprRef` still exposed cached expression text, tokens, identifiers, root nodes, and spans as public fields. That made source-preserving expression internals visible to downstream analysis even though expression users should consume a parsed expression reference through a stable API. | `DONE` | Made `WgslExprRef` opaque and moved parser/analysis/tests to named accessors that copy token and identifier arrays before returning them. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-145` | Expression token API hardening | `WgslExprRef` returned copied expression tokens, but each `WgslExprToken` still exposed kind, text, and span fields directly, so downstream code could still depend on expression lexer storage layout. | `DONE` | Made expression tokens opaque and moved parser, analysis normalization, and white-box tests to token accessor methods. Verified with `moon check --target all --deny-warn`, `moon test`, README JS tests, WGSL validation, preprocess parity, and `moon ide analyze` on 2026-05-06. |
| `WGSL-146` | Analysis/compose semantic boundary hardening | Compose still inspected emitted declaration source text with `struct ` / `alias ` prefixes to decide whether a nested flattened import name should be recorded. That was a source-level semantic check outside the analysis owner. | `DONE` | Removed emitted declaration text from the public analysis result and replaced it with an AST-derived `is_type_declaration` property. Compose now consumes the semantic property directly. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-147` | Type AST source-cache hardening | `WgslTypeRef` still exposed `name`, `arguments`, `start`, and cached raw type text, leaving parser source-span trivia in the public AST and allowing callers to depend on type whitespace. | `DONE` | Made `WgslTypeRef` opaque, removed the raw type-text cache, added read-only structural accessors plus canonical type text, and changed template-list parsing/tests to structural canonical output. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-148` | Composer registration result API pruning | `ComposableModuleDefinition` still exposed unused count/list accessors that duplicated more precise semantic query methods and made registration storage look like a stable DTO contract. | `DONE` | Removed `shader_def_count`, `effective_defs`, and `all_import_count`; callers now have only exact `shader_def`, `contains_effective_def`, and import membership/emptiness queries. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-149` | Expression AST source-cache hardening | `WgslExprRef` still stored and exposed the raw expression source slice even though production analysis consumes tokens, identifiers, root nodes, and spans. | `DONE` | Removed the cached expression text from `WgslExprRef` and updated white-box tests to assert source spans explicitly through the original test source instead of an AST text cache. Verification is tracked with this refactor. |
| `WGSL-150` | Lexer token API hardening | `lex.WgslToken` still exposed public enum constructors and a public token-kind enum, so parser and metadata code could couple directly to lexer storage layout instead of named token semantics. | `DONE` | Replaced `WgslToken` with an opaque struct, hid token construction and token-kind storage inside `lex`, and migrated parser, metadata, canonical writer, and lexer tests to intent-level query methods. Verified with the full local gate on 2026-05-06. |
| `WGSL-151` | AST directive API hardening | `WgslDirective` still exposed directive kind, tail text, and parser span fields directly, letting downstream code depend on the directive node storage layout instead of the AST API boundary. | `DONE` | Made directive fields private and migrated parser, metadata, compose, preprocess, analysis, and tests to named directive accessors. Verified with the full local gate on 2026-05-06. |
| `WGSL-152` | AST attribute API hardening | `WgslAttribute` still exposed attribute name, argument text, parsed argument ref, and parser spans as public fields, leaving another source-text AST storage dependency on the public surface. | `DONE` | Made attribute fields private, migrated analysis and parser tests to accessors, and updated the moonyacc grammar action to use accessor-based attribute spans. Verified with the full local gate on 2026-05-06. |
| `WGSL-153` | Statement AST storage hardening | Statement, block, local-declaration, and statement-expression nodes still exposed parser-owned storage arrays and raw statement text directly, preserving a source-level statement DTO path. | `DONE` | Made those statement-layer records opaque, added read-only accessors with copied arrays, and migrated analysis plus parser white-box tests to the accessor boundary. Verified with the full local gate on 2026-05-06. |
| `WGSL-154` | Declaration AST storage hardening | Top-level declaration nodes still exposed function, struct, global, const, override, type-alias, and translation-unit storage fields directly, so downstream packages could couple to parser-owned declaration layout. | `DONE` | Made declaration-layer records opaque, added read-only accessors with copied arrays where needed, and migrated parser, metadata, analysis, compose, and white-box tests to the accessor boundary. Verified with the full local gate on 2026-05-06. |
| `WGSL-155` | Identifier/expression-node AST hardening | `WgslIdent` and `WgslExprNode` still exposed their name/span/node-kind storage fields, leaving the lowest-level AST layout visible after expression refs and declaration nodes were made opaque. | `DONE` | Made identifier and expression-node fields private, added read-only accessors, regenerated the private moonyacc parser from accessor-based grammar actions, and migrated parser, metadata, analysis, compose, and white-box tests. Verified with the full local gate on 2026-05-06. |
| `WGSL-156` | Statement source-cache removal | `WgslStatement` still cached and exposed raw statement source text solely for white-box assertions, keeping a statement-level source-preserving DTO path alive after semantic statement payloads were available. | `DONE` | Removed statement raw-text storage and the public `text` accessor, tightened the statement constructor, and converted white-box coverage to assert structural statement payloads and bindings instead of source slices. Verified with the full local gate on 2026-05-06. |
| `WGSL-157` | Declaration source-cache removal | `WgslGlobalDecl` still stored and exposed raw top-level declaration text, forcing downstream analysis to treat parsed declarations as source-slice DTOs. | `DONE` | Removed declaration raw-text storage from the public AST, added an end-span accessor, and changed analysis graph construction to derive any remaining private source slice from the original source plus declaration spans. Verified with the full local gate on 2026-05-06. |
| `WGSL-158` | Source-preserving writeback removal | Analysis and metadata still had source-level writeback escape hatches: analysis graph blocks carried declaration text, virtual override/rename emission rewrote source slices, metadata import substitution used the shared source rewriter, and `lex` still exported the old canonical writer. | `DONE` | Replaced analysis graph text with absolute token streams, routed rename/virtual/import-substitution emission through canonical token-stream emitters driven by AST-collected identifier spans, fixed override-target rewriting to keep non-function blocks, and deleted the obsolete `lex/canonical_writer.mbt` API. Verified with the full local gate on 2026-05-06. |
| `WGSL-159` | Token emitter convergence | After source-preserving writeback removal, `analysis` and `metadata` still duplicated canonical token emission and spacing rules, risking divergent WGSL output for rename and import-substitution paths. | `DONE` | Moved canonical token-stream emission and absolute-fragment tokenization into `lex/token_emitter.mbt`, replaced package-local emitters with a shared opaque `WgslTokenReplacement` API, and kept analysis/metadata responsible only for AST-derived replacement planning. Verified with `moon check --target all --deny-warn` and `moon test` on 2026-05-06; full gate tracked with this refactor. |
| `WGSL-160` | Declaration scalar source-cache removal | Declaration AST nodes still cached type and initializer text alongside structured `WgslTypeRef` / `WgslExprRef` payloads, leaving a scalar source-slice path that white-box tests could depend on. | `DONE` | Removed stored type/initializer source strings from function-result, local, struct-member, global, const, override, and alias nodes; parser tokens now require structured type/expression refs before constructing typed declarations. Verified with the full local gate on 2026-05-06. |
| `WGSL-161` | AST string convenience API removal | Even after scalar source-cache removal, AST still exposed convenience string views for attribute arguments and declaration types, so tests and downstream code could continue to treat structured nodes as text DTOs. | `DONE` | Removed `WgslAttribute::arguments` and declaration `ty()` accessors, changed attribute parser tokens to carry only `WgslExprRef` plus end span, and migrated white-box coverage to structural type/expression assertions. Verified with the full local gate on 2026-05-06. |
| `WGSL-162` | Template argument AST conversion | `var<...>` template lists were still represented as `Array[String]`, which hid template identifiers from AST-driven dependency collection and kept a declaration subfield as a text DTO. | `DONE` | Changed local/global variable template lists to `Array[WgslTypeRef]`, made the grammar return structured template refs, updated normalization keys, and added template arguments to AST identifier collection. Verified with the full local gate on 2026-05-06. |
| `WGSL-163` | Local declaration kind enum | Local declaration kind was still stored as raw strings (`var`, `let`, `const`), leaving a finite WGSL syntax category as stringly typed AST state. | `DONE` | Added `WgslLocalDeclarationKind`, changed local binding AST construction and normalization to consume the enum, and migrated parser white-box assertions to the structured kind. Verified with the full local gate on 2026-05-06. |
| `WGSL-164` | Function qualifier enum | Virtual/override function qualifiers were still stored as raw strings, so virtual override analysis and identifier collection depended on text equality for a finite parser-owned category. | `DONE` | Added `WgslFunctionQualifier`, changed declaration-head parsing and function AST construction to carry `WgslFunctionQualifier?`, and migrated normalization, identifier collection, and virtual override analysis to enum matching. Verified with the full local gate on 2026-05-06. |
| `WGSL-165` | Argument/member identifier AST conversion | Function arguments and struct members still split identifiers into raw name strings plus name spans, keeping parser-owned identifier structure outside `WgslIdent`. | `DONE` | Changed `WgslFunctionArgument` and `WgslStructMember` to store `WgslIdent`, removed their name-span accessors, and migrated parser grammar actions plus analysis/test callers to consume structured identifier nodes. Verified with the full local gate on 2026-05-06. |
| `WGSL-166` | Directive identifier AST conversion | `WgslDirective` still split directive identifiers into raw name strings plus name spans, leaving directive parsing as a source-span DTO instead of a structured identifier node. | `DONE` | Changed `WgslDirective` to store `WgslIdent`, removed directive name-span accessors, and migrated directive scan, normalization, grammar actions, and white-box tests to consume structured directive identifiers. Verified with the full local gate on 2026-05-06. |
| `WGSL-167` | Attribute identifier AST conversion | `WgslAttribute` still stored attribute names as raw strings even though parser tokens already carry structured identifier spans. | `DONE` | Changed `WgslAttribute` to store `WgslIdent`, updated moonyacc attribute actions, and migrated normalization, identifier policy, and parser white-box assertions to consume structured attribute identifiers. Verified with the full local gate on 2026-05-06. |
| `WGSL-168` | Shader IR lowering | The new Naga-style IR package initially exposed only the IR data model; WGSL AST parsing could not yet produce IR modules, so declaration, type, binding, and function-body semantics still stopped at the AST boundary. | `DONE` | Added an AST-driven WGSL-to-IR lowerer that parses strict translation units into module arenas, lowers scalar/vector/matrix/array/pointer/resource types, attributes, globals, constants, overrides, entry points, local variables, expressions, stores, returns, calls, loops, and conditionals without source-level writeback. Added white-box coverage for bindings, entry-point metadata, stable function handles, and unresolved identifier failure. Verified with `moon check ir --deny-warn`, `moon test ir`, and `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-204` | IR emitter | The IR package can now lower WGSL AST into module arenas, but there is no canonical IR-to-WGSL emitter. That blocks an actual `AST -> IR -> emit` pipeline and leaves compose/export unable to use IR as the single source of truth. | `DONE` | Added deterministic IR-to-WGSL emission for structs, constants, overrides, globals, helper functions, entry points, function locals, returns, stores, calls, conditionals, loops, and the lowered expression subset. The emitter reads only IR arenas and fails on unsupported/invalid shapes instead of fabricating WGSL. Verified with `moon check ir --deny-warn` and `moon test ir` on 2026-05-06. |
| `WGSL-205` | IR validation | The IR package has data structures and a lowerer, but no internal validation pass for invalid handles, invalid entry-point metadata, unsupported placeholder function references, or type/expression arena consistency. | `DONE` | Added `validate_wgsl_ir_module`, covering type/global/function/local/expression handles, handle ranges, function-call targets, entry-point names, and statement expression references. `emit_wgsl_module_from_ir` now validates before emitting. Verified with `moon check ir --deny-warn` and `moon test ir` on 2026-05-06. |
| `WGSL-206` | IR symbol table linking | Composer still owns a separate semantic symbol graph and final-name logic; IR identities are not yet the canonical binding table for import reachability, final-name allocation, and reference rewriting. | `DONE` | Moved stable symbol identities, canonical/alias final-name tables, import edges, and reachability graph storage into the IR package as opaque linking primitives. Compose now consumes IR identities and IR link graphs instead of defining compose-local identity/graph records. The remaining source-origin final-name reconciliation was replaced with an identity-backed final-name application plan keyed by `(rel_path, original declaration name)`, and the old public string-map declaration rewrite API was deleted. Production `parse -> lower -> link -> emit` migration remains tracked separately as `WGSL-207`. Verified with `moon check --target all --deny-warn` on 2026-05-06. |
| `WGSL-207` | Composer IR pipeline | Compose/export still exposed duplicate explicit `_ir` public APIs after the default path became IR-backed, keeping a misleading two-pipeline surface. | `DONE` | Deleted `Composer::prepare_wgsl_source_ir`, `Composer::compose_wgsl_ir`, `Composer::compose_wgsl_source_ir`, and `export_wgsl_with_options_ir`; migrated validation tools, compose-case tooling, and tests to the default compose/export APIs. The only production output path now prepares composed source, lowers it to IR, validates IR, and emits final WGSL from IR before catalogs/maps are built. Verified with `moon check --target all --deny-warn`, `moon info`, `tools/check_ir_roundtrip_corpus.sh`, and `tools/check_moon_wgsl_byte_parity.sh` on 2026-05-07. |
| `WGSL-208` | Complete WGSL lowering coverage | The WGSL AST lowerer needed an enforced corpus boundary instead of open-ended “edge forms remain” wording. | `DONE` | The current IR lowerer coverage is locked by the local IR corpus, the upstream compose validation corpus, the manifest-driven WGSL corpus matrix, and the official CTS gate. The local matrix now exact-gates 54 total cases, 20 compose cases, 43 parse cases, 42 Naga source validations, 47 IR roundtrips, 46 emitted-IR Naga validations, and 1 runtime-valid compose case; its main and runtime-valid manifests reject malformed schema rows and duplicate IDs. The official gate parses every extracted official case, IR-roundtrips every case, and runs naga validation for every case not blocked by the pinned naga WGSL frontend. Future WGSL forms must enter through a failing corpus fixture rather than an untracked TODO. Verified with `tools/check_wgsl_corpus_matrix.sh` and `tools/check_official_wgsl_corpus.sh` on 2026-05-10. |
| `WGSL-209` | Official WGSL corpus gate | Local parser/lowerer tests did not require the extracted official corpus to be exactly accounted for. | `DONE` | Hardened `tools/check_official_wgsl_corpus.sh` to require 114 parsed GPUWeb CTS static valid cases and 111 IR-roundtripped/naga-validated cases at the checked CTS ref. Added `testdata/gpuweb_cts_ir_blocked_by_oracle.txt` for the three extracted `enable subgroups;` cases blocked by the pinned naga validation oracle. The script originally used an exact allowlist manifest; `WGSL-215` later removed that allowlist so every extracted static-valid case is now an IR gate input by construction. Verified with `tools/check_official_wgsl_corpus.sh` on 2026-05-07. |
| `WGSL-210` | Byte-exact naga_oil parity gate | Byte parity did not gate the atomics fixture because upstream expected bytes contain an internal compare-exchange result type annotation that is not valid runtime WGSL. | `DONE` | Added an explicit naga-oil writer parity profile to the IR emitter and compose-case tool, changed the byte parity gate to diff atomics against upstream bytes without normalization, and kept the default runtime compose path separately validated with Naga. Existing byte cases now all use direct `diff -u`; the manifest has no special byte class. Verified with `tools/check_moon_wgsl_byte_parity.sh` on 2026-05-09. |

## Current work queue

- `WGSL-281`: Burn external compose byte drift to zero, then remove legacy drift mode/manifests.
- `WGSL-282`: Retain imported module declarations according to pinned `naga_oil` module construction, using structured graph/IR data only.
- `WGSL-283`: Replace moon_wgsl compatibility writer policy with pinned-Naga-compatible writer behavior.
  - Latest: pointer-valued builtin arguments now share the Naga-style `(&expr)` spelling policy across atomic calls and `arrayLength`, removing the `arrayLength(&x)` vs `arrayLength((&x))` writer-policy split.
  - Latest: member/index access through pointer-typed expression bases now emits Naga-style explicit `(*ptr)` dereference spelling, and the naga-oil writer profile contextualizes integer constructor arguments so concrete integer constructors preserve `u`/`i` suffixes instead of leaking abstract integer spelling.
  - Latest: naga-oil writer numeric literal contextualization now applies to `select` operands and user-function call arguments as well as constructors, so typed scalar call sites emit concrete `f`/`u`/`i` literal spelling instead of leaking abstract literal spelling.
  - Latest: vector splats, vector constructors, and same-type math builtin arguments now inherit component scalar context under the naga-oil writer policy, aligning `vecN(0.0)` / `vecN(-1.0)` style output with Naga's concrete `vecN(0f)` / `vecN(-1f)` spelling.
  - Latest: abstract composite constructors are now rebound to concrete sibling argument types during lowering for homogeneous math calls, and the naga-oil writer profile emits Naga's implicit `@interpolate(flat)` spelling for integer `@location` IO.
  - Latest: naga-oil writer profile now emits Naga-style bracketed non-struct `AccessIndex` expressions and compact storage texture type arguments, aligning vector/matrix projections such as `m[0][2]` and storage texture spellings such as `texture_storage_2d<rgba8unorm,write>`.
  - Latest: selected-item compose now follows naga-oil's recursive module import semantics instead of slicing only locally referenced directives. Final-name cache entries no longer imply that a selected item was emitted, and extraction-origin identities are recorded as emitted so dependency declarations such as `ColorGrading` are not duplicated later. External Bevy compose parity improved to 104 writer-exact cases and 45 known writer drifts.
  - Latest: generated-import array constants now inline only for imported generated constants while root module array constants still emit, and selected-slice extraction now keeps original dependency traversal while normalizing only const/override declarations by source-module order. This removes Bevy `pbr_functions.wgsl` and `shadows.wgsl` from writer drift. Current external naga-oil compose parity is 105 writer-exact / 44 writer drifts / 53 byte-exact / 96 byte drifts.
  - Latest: generated-import array constants are now inlined in the naga-oil writer profile only for imported generated constants, matching Naga's handling of selected-import array constants while preserving root module array const declarations. Selected-slice extraction now separates reachability from constant declaration order: dependency traversal still orders functions, but const/override declarations retain source-module order before IR lowering. This removes Bevy `pbr_functions.wgsl` and `shadows.wgsl` from writer drift; strict external Bevy compose parity now reports 105 writer-exact cases and 44 known writer drifts, with 96 byte drifts still tracked.
  - Latest: import request declared-dependency ordering now mutates the execution request queue instead of returning an ignored ordered copy, and IR struct predeclaration now topologically emits field type dependencies before owner structs. This removes Bevy `meshlet_bindings.wgsl` from writer drift while preserving the strict pipeline-interface subset gate. Strict external Bevy compose parity now reports 109 writer-exact cases, 40 writer drifts, and 95 byte drifts.
  - Latest: direct explicit leaf imports now feed structured generated-import provenance for naga-oil local spelling, so lowered function arguments and local declarations can intentionally use the imported generated name without source-level rewriting. The emitter name table also skips duplicate ordinary-function allocation for entry-point functions. This removes Bevy `atmosphere/functions.wgsl` and `render/mesh.wgsl` from writer drift; strict external Bevy compose parity now reports 111 writer-exact cases, 38 writer drifts, and 95 byte drifts.
- `WGSL-284`: Add byte-level diagnostic parity for compose/preprocess failures.
- `WGSL-254`: Continue expanding real-project WGSL corpora and profile shapes beyond the current five-repository gate while keeping `skipped=0`, `expected-failures=0`, manifest-owned expected-invalid classifications, manifest-owned raw-vs-compose profile execution modes, exact per-repository corpus counts, and strict TSV schema validation.
