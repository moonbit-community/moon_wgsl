# Defer the WESL module until real semantics exist

The workspace will reserve an architectural position for a future WESL extension layer, but it will not create or publish an empty WESL module during the initial split. A WESL module should appear only when concrete WESL syntax, composition, or extension semantics exist.

